{
 "info": {
  "name": "PayMesh API",
  "description": "PayMesh Payment-as-a-Service API.\n\nSet `baseUrl` (defaults to http://localhost:8080). Run the folders top to bottom: onboarding creates a merchant, Identity & Auth attaches a user and captures a token, and Authenticated access uses it.\n\nCapabilities covered: Identity & Auth, Merchant, Customer, Order, Payment Intents, and the shared transactional outbox (ADR-010).",
  "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
 },
 "variable": [
  {
   "key": "baseUrl",
   "value": "http://localhost:8080"
  },
  {
   "key": "merchantId",
   "value": ""
  },
  {
   "key": "authEmail",
   "value": ""
  },
  {
   "key": "accessToken",
   "value": ""
  },
  {
   "key": "refreshToken",
   "value": ""
  },
  {
   "key": "firstRefreshToken",
   "value": ""
  },
  {
   "key": "wrongPasswordBody",
   "value": ""
  },
  {
   "key": "customerId",
   "value": ""
  },
  {
   "key": "otherEmail",
   "value": ""
  },
  {
   "key": "otherMerchantId",
   "value": ""
  },
  {
   "key": "otherUserEmail",
   "value": ""
  },
  {
   "key": "otherAccessToken",
   "value": ""
  },
  {
   "key": "forgedToken",
   "value": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ1c3JfMTExMTExMTEtMTExMS00MTExLTgxMTEtMTExMTExMTExMTExIiwiaXNzIjoicGF5bWVzaCIsImV4cCI6OTk5OTk5OTk5OX0.ZmFrZS1zaWduYXR1cmUtbm90LXByb2R1Y2VkLWJ5LXRoZS1yZWFsLWtleQ"
  },
  {
   "key": "orderId",
   "value": ""
  },
  {
   "key": "linkedOrderId",
   "value": ""
  },
  {
   "key": "otherCustomerId",
   "value": ""
  },
  {
   "key": "ghostCustomerId",
   "value": ""
  },
  {
   "key": "orderCursor",
   "value": ""
  },
  {
   "key": "firstPageId",
   "value": ""
  },
  {
   "key": "firstOrderBody",
   "value": ""
  },
  {
   "key": "cancelledBody",
   "value": ""
  },
  {
   "key": "strangerCustomerBody",
   "value": ""
  },
  {
   "key": "createOrderKey",
   "value": ""
  },
  {
   "key": "dupRefKey",
   "value": ""
  },
  {
   "key": "zeroAmountKey",
   "value": ""
  },
  {
   "key": "linkedOrderKey",
   "value": ""
  },
  {
   "key": "strangerCustomerKey",
   "value": ""
  },
  {
   "key": "ghostCustomerKey",
   "value": ""
  },
  {
   "key": "cancelKey",
   "value": ""
  },
  {
   "key": "secondCancelKey",
   "value": ""
  },
  {
   "key": "crossCancelKey",
   "value": ""
  },
  {
   "key": "outboxOrderId",
   "value": ""
  },
  {
   "key": "outboxOrderKey",
   "value": ""
  },
  {
   "key": "outboxDuplicateKey",
   "value": ""
  },
  {
   "key": "paymentIntentId",
   "value": ""
  },
  {
   "key": "firstIntentBody",
   "value": ""
  },
  {
   "key": "createIntentKey",
   "value": ""
  },
  {
   "key": "secondIntentKey",
   "value": ""
  },
  {
   "key": "mismatchAmountKey",
   "value": ""
  },
  {
   "key": "crossTenantIntentKey",
   "value": ""
  },
  {
   "key": "cancelIntentKey",
   "value": ""
  },
  {
   "key": "secondCancelIntentKey",
   "value": ""
  },
  {
   "key": "confirmOrderId",
   "value": ""
  },
  {
   "key": "confirmIntentId",
   "value": ""
  },
  {
   "key": "guardOrderId",
   "value": ""
  },
  {
   "key": "guardIntentId",
   "value": ""
  },
  {
   "key": "attachedBody",
   "value": ""
  },
  {
   "key": "confirmedBody",
   "value": ""
  },
  {
   "key": "providerCallbackSecret",
   "value": ""
  },
  {
   "key": "callbackBody",
   "value": ""
  },
  {
   "key": "callbackSignature",
   "value": ""
  },
  {
   "key": "cbOrderId",
   "value": ""
  },
  {
   "key": "cbOrderKey",
   "value": ""
  },
  {
   "key": "cbIntentId",
   "value": ""
  },
  {
   "key": "cbIntentKey",
   "value": ""
  },
  {
   "key": "cbAttachKey",
   "value": ""
  },
  {
   "key": "cbConfirmKey",
   "value": ""
  },
  {
   "key": "cbReconfirmKey",
   "value": ""
  },
  {
   "key": "cbReference",
   "value": ""
  },
  {
   "key": "cbOccurredAt",
   "value": ""
  },
  {
   "key": "cbEarlierAt",
   "value": ""
  },
  {
   "key": "cbLaterAt",
   "value": ""
  },
  {
   "key": "cbChallengeEventId",
   "value": ""
  },
  {
   "key": "cbOrder2Id",
   "value": ""
  },
  {
   "key": "cbOrder2Key",
   "value": ""
  },
  {
   "key": "cbSecondIntentId",
   "value": ""
  },
  {
   "key": "cbIntent2Key",
   "value": ""
  },
  {
   "key": "cbAttach2Key",
   "value": ""
  },
  {
   "key": "cbConfirm2Key",
   "value": ""
  },
  {
   "key": "cbCancel2Key",
   "value": ""
  },
  {
   "key": "capOrderKey",
   "value": ""
  },
  {
   "key": "capOrderId",
   "value": ""
  },
  {
   "key": "capIntentKey",
   "value": ""
  },
  {
   "key": "capIntentId",
   "value": ""
  },
  {
   "key": "capAttachKey",
   "value": ""
  },
  {
   "key": "capEarlyKey",
   "value": ""
  },
  {
   "key": "capConfirmKey",
   "value": ""
  },
  {
   "key": "capReference",
   "value": ""
  },
  {
   "key": "capOverKey",
   "value": ""
  },
  {
   "key": "capZeroKey",
   "value": ""
  },
  {
   "key": "capPartialKey",
   "value": ""
  },
  {
   "key": "capSecondKey",
   "value": ""
  },
  {
   "key": "capCancelKey",
   "value": ""
  },
  {
   "key": "capOrder2Key",
   "value": ""
  },
  {
   "key": "capOrder2Id",
   "value": ""
  },
  {
   "key": "capIntent2Key",
   "value": ""
  },
  {
   "key": "capIntent2Id",
   "value": ""
  },
  {
   "key": "capAttach2Key",
   "value": ""
  },
  {
   "key": "capConfirm2Key",
   "value": ""
  },
  {
   "key": "capReference2",
   "value": ""
  },
  {
   "key": "capCancel2Key",
   "value": ""
  },
  {
   "key": "capIntent3Key",
   "value": ""
  },
  {
   "key": "capCrossKey",
   "value": ""
  },
  {
   "key": "simulatorKey",
   "value": "dev-only-insecure-simulator-api-key-change-me"
  },
  {
   "key": "simPaymentId",
   "value": ""
  },
  {
   "key": "simManualId",
   "value": ""
  },
  {
   "key": "simToday",
   "value": ""
  },
  {
   "key": "simKeyA",
   "value": ""
  },
  {
   "key": "simKeyB",
   "value": ""
  },
  {
   "key": "simKeyC",
   "value": ""
  },
  {
   "key": "simSuccessKey",
   "value": ""
  },
  {
   "key": "simDeclineKey",
   "value": ""
  },
  {
   "key": "simTimeoutKey",
   "value": ""
  },
  {
   "key": "simManualKey",
   "value": ""
  },
  {
   "key": "simCaptureKey",
   "value": ""
  },
  {
   "key": "simCapture2Key",
   "value": ""
  },
  {
   "key": "simCapture3Key",
   "value": ""
  },
  {
   "key": "simRefundKey",
   "value": ""
  },
  {
   "key": "simRefund2Key",
   "value": ""
  },
  {
   "key": "simE2eRef",
   "value": ""
  },
  {
   "key": "simE2eOrderKey",
   "value": ""
  },
  {
   "key": "simE2eOrderId",
   "value": ""
  },
  {
   "key": "simE2eIntentKey",
   "value": ""
  },
  {
   "key": "simE2eIntentId",
   "value": ""
  },
  {
   "key": "simE2eConfirmKey",
   "value": ""
  },
  {
   "key": "simE2ePayKey",
   "value": ""
  },
  {
   "key": "simE2ePolls",
   "value": ""
  },
  {
   "key": "lgOrderKey",
   "value": ""
  },
  {
   "key": "lgOrderId",
   "value": ""
  },
  {
   "key": "lgIntentKey",
   "value": ""
  },
  {
   "key": "lgIntentId",
   "value": ""
  },
  {
   "key": "lgAttachKey",
   "value": ""
  },
  {
   "key": "lgConfirmKey",
   "value": ""
  },
  {
   "key": "lgReference",
   "value": ""
  },
  {
   "key": "lgBalanceBefore",
   "value": ""
  },
  {
   "key": "lgPollAttempt",
   "value": ""
  },
  {
   "key": "rfOrderKey",
   "value": ""
  },
  {
   "key": "rfOrderId",
   "value": ""
  },
  {
   "key": "rfIntentKey",
   "value": ""
  },
  {
   "key": "rfIntentId",
   "value": ""
  },
  {
   "key": "rfAttachKey",
   "value": ""
  },
  {
   "key": "rfConfirmKey",
   "value": ""
  },
  {
   "key": "rfRefundKey",
   "value": ""
  },
  {
   "key": "rfPartialKey",
   "value": ""
  },
  {
   "key": "rfOverKey",
   "value": ""
  },
  {
   "key": "rfCancelKey",
   "value": ""
  },
  {
   "key": "rfCrossKey",
   "value": ""
  },
  {
   "key": "rfRefundId",
   "value": ""
  },
  {
   "key": "rfPollAttempt",
   "value": ""
  },
  {
   "key": "refundCallbackSecret",
   "value": ""
  },
  {
   "key": "refundCallbackBody",
   "value": ""
  },
  {
   "key": "refundCallbackSignature",
   "value": ""
  },
  {
   "key": "rfBalanceBefore",
   "value": ""
  },
  {
   "key": "simE2eAttachKey",
   "value": ""
  },
  {
   "key": "lgSettleLast",
   "value": ""
  },
  {
   "key": "lgSettleTries",
   "value": ""
  },
  {
   "key": "rfSettleLast",
   "value": ""
  },
  {
   "key": "rfSettleTries",
   "value": ""
  },
  {
   "key": "whSlug",
   "value": ""
  },
  {
   "key": "whEndpointId",
   "value": ""
  },
  {
   "key": "whSecret",
   "value": ""
  },
  {
   "key": "whRotatedSecret",
   "value": ""
  },
  {
   "key": "whOrderKey",
   "value": ""
  },
  {
   "key": "whOrderId",
   "value": ""
  },
  {
   "key": "whIntentKey",
   "value": ""
  },
  {
   "key": "whIntentId",
   "value": ""
  },
  {
   "key": "whAttachKey",
   "value": ""
  },
  {
   "key": "whConfirmKey",
   "value": ""
  },
  {
   "key": "whReplayKey",
   "value": ""
  },
  {
   "key": "whDeliveryId",
   "value": ""
  },
  {
   "key": "whPollAttempt",
   "value": ""
  }
 ],
 "item": [
  {
   "name": "Health",
   "item": [
    {
     "name": "Actuator health",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/actuator/health",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "actuator",
        "health"
       ]
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200', () => pm.response.to.have.status(200));",
         "pm.test('UP', () => pm.expect(pm.response.json().status).to.eql('UP'));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Merchant (public onboarding)",
   "description": "Creating a merchant is the signup step and needs no token -- it precedes having an account at all. Reading one does need a token; those requests live in 'Authenticated access' below, after a session exists.",
   "item": [
    {
     "name": "Register merchant",
     "request": {
      "method": "POST",
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"businessName\": \"  FreshBrew Cafe  \",\n  \"email\": \" Owner@FreshBrew.Example \",\n  \"country\": \"in\",\n  \"defaultCurrency\": \"inr\"\n}"
      },
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants"
       ]
      },
      "description": "Input is deliberately untrimmed and mixed-case: the domain trims the business name, lowercases the email, and uppercases country and currency."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// Unique email per run so the unique constraint does not fail a repeat run.",
         "pm.collectionVariables.set('runEmail', 'owner+' + Date.now() + '@freshbrew.example');",
         "const body = JSON.parse(pm.request.body.raw);",
         "body.email = pm.collectionVariables.get('runEmail');",
         "pm.request.body.raw = JSON.stringify(body);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const body = pm.response.json();",
         "pm.test('opaque prefixed id', () => pm.expect(body.id).to.match(/^mrc_[0-9a-f-]{36}$/));",
         "pm.test('business name trimmed', () => pm.expect(body.businessName).to.eql('FreshBrew Cafe'));",
         "pm.test('email lowercased', () => pm.expect(body.email).to.eql(body.email.toLowerCase()));",
         "pm.test('country uppercased', () => pm.expect(body.country).to.eql('IN'));",
         "pm.test('currency uppercased', () => pm.expect(body.defaultCurrency).to.eql('INR'));",
         "pm.test('starts pending verification', () => pm.expect(body.status).to.eql('PENDING_VERIFICATION'));",
         "pm.test('timestamps are UTC ISO-8601', () => pm.expect(body.createdAt).to.match(/Z$/));",
         "pm.collectionVariables.set('merchantId', body.id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Activate the merchant (200, PLATFORM_ADMIN)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants/{{merchantId}}/activate",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants",
        "{{merchantId}}",
        "activate"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{platformAdminToken}}"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"KYC cleared in the sandbox\"\n}"
      },
      "description": "WITHOUT THIS REQUEST THE REST OF THIS COLLECTION 403s, AND SINCE ADR-021 IT ALWAYS HAS.\n\nMerchant.register lands on PENDING_VERIFICATION, not ACTIVE. MerchantStatusFilter then refuses every merchant-scoped write with MERCHANT_NOT_ACTIVE, so a freshly onboarded merchant can create no customer, no order and no payment. That is the intended product behaviour -- onboarding is not approval -- and the collection simply never caught up with it.\n\nACTIVATION IS PLATFORM STAFF'S ACT, NOT THE MERCHANT'S. A merchant able to lift its own suspension would make suspension advisory (ADR-021), so this route requires PLATFORM_ADMIN and is exempt from the status filter -- it necessarily acts on a merchant that is not yet ACTIVE.\n\nWHERE THE TOKEN COMES FROM, SAID PLAINLY. PLATFORM_ADMIN IS grantable since ADR-027 -- V23 made user_roles.merchant_id nullable, POST /api/v1/users/{userId}/platform-admin promotes somebody, and the FIRST one comes from the startup property paymesh.security.bootstrap-platform-admin-email. A collection cannot restart the application, so it cannot walk that path; the pre-request script still MINTS an HS256 token with the dev signing secret, which is published in this repository. Same thing this collection already does when it signs provider callbacks with the dev HMAC secret.\n\nWHAT DID CHANGE IS THE CLAIM. A platform role now travels with NO ':merchantId' suffix, and the suffixed form is DISCARDED rather than read as platform authority -- it was the shape a merchant admin would produce promoting themselves out of their own tenant. This mint emits the bare 'PLATFORM_ADMIN', which is exactly what JwtAccessTokenService now issues for a real grant.\n\nIT ONLY WORKS ON THE dev PROFILE. Outside it DevelopmentSecretGuard refuses to start on that secret at all, which is precisely the protection that makes doing this here acceptable."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// Mint a PLATFORM_ADMIN access token, signed with the DEV secret.",
         "// Claims copied from JwtAccessTokenService: issuer 'paymesh', the user id as subject, and a",
         "// 'roles' claim of ROLE:merchantId strings -- the scope is carried per role because one user",
         "// can hold roles at several merchants.",
         "//",
         "// NO MERCHANT SUFFIX. Since ADR-027 the ABSENCE of the ':merchantId' is what claims this role",
         "// is platform-wide. The suffixed form is now DISCARDED rather than honoured: it was the",
         "// escalation shape, a merchant admin promoting themselves out of their own tenant.",
         "const secret = 'dev-only-insecure-jwt-signing-secret-change-me';",
         "const now = Math.floor(Date.now() / 1000);",
         "",
         "const b64url = (w) => CryptoJS.enc.Base64.stringify(w)",
         "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
         "const seg = (o) => b64url(CryptoJS.enc.Utf8.parse(JSON.stringify(o)));",
         "",
         "const header = seg({ alg: 'HS256', typ: 'JWT' });",
         "const payload = seg({",
         "    iss: 'paymesh',",
         "    sub: 'usr_00000000-0000-0000-0000-0000000000ad',",
         "    iat: now,",
         "    exp: now + 900,",
         "    email: 'platform-admin@paymesh.test',",
         "    roles: ['PLATFORM_ADMIN']",
         "});",
         "",
         "const signature = b64url(CryptoJS.HmacSHA256(header + '.' + payload, secret));",
         "pm.collectionVariables.set('platformAdminToken', header + '.' + payload + '.' + signature);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('ACTIVE -- every merchant-scoped folder below depends on this', () =>",
         "    pm.expect(pm.response.json().status).to.eql('ACTIVE'));",
         "",
         "// Loud on failure: everything after this point fails with MERCHANT_NOT_ACTIVE and the cause",
         "// is here, hundreds of requests earlier.",
         "if (pm.response.code !== 200) {",
         "    console.error('ACTIVATION FAILED (' + pm.response.code + '). Every merchant-scoped request ' +",
         "                  'below will now 403 with MERCHANT_NOT_ACTIVE. Is the app on the dev profile?');",
         "}"
        ]
       }
      }
     ]
    },
    {
     "name": "Register merchant - duplicate email (409)",
     "request": {
      "method": "POST",
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"businessName\": \"FreshBrew Again\",\n  \"email\": \"{{runEmail}}\",\n  \"country\": \"IN\",\n  \"defaultCurrency\": \"INR\"\n}"
      },
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants"
       ]
      },
      "description": "Reuses the email from 'Register merchant'. Run that request first."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('MERCHANT_EMAIL_ALREADY_EXISTS'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Register merchant - blank fields (400)",
     "request": {
      "method": "POST",
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"businessName\": \"\",\n  \"email\": \"\",\n  \"country\": \"\",\n  \"defaultCurrency\": \"\"\n}"
      },
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants"
       ]
      },
      "description": "Bean Validation failure: returns VALIDATION_FAILED with a populated fieldErrors map."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "const body = pm.response.json();",
         "pm.test('code', () => pm.expect(body.code).to.eql('VALIDATION_FAILED'));",
         "pm.test('reports every blank field', () => pm.expect(Object.keys(body.fieldErrors)).to.have.lengthOf(4));"
        ]
       }
      }
     ]
    },
    {
     "name": "Register merchant - invalid country (400)",
     "request": {
      "method": "POST",
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"businessName\": \"Bad Country Co\",\n  \"email\": \"owner@badcountry.example\",\n  \"country\": \"USA\",\n  \"defaultCurrency\": \"USD\"\n}"
      },
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants"
       ]
      },
      "description": "Domain invariant failure, which Bean Validation cannot express: returns INVALID_REQUEST with an empty fieldErrors map."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "const body = pm.response.json();",
         "pm.test('code', () => pm.expect(body.code).to.eql('INVALID_REQUEST'));",
         "pm.test('names the broken rule', () => pm.expect(body.message).to.eql('Country must be of length 2'));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Identity & Auth",
   "description": "Registration, login, and rotating refresh tokens.\n\nRun the requests in order: each depends on tokens captured by the previous one. The reuse request deliberately revokes the whole family, so re-run from 'Register user' to get a working session again.",
   "item": [
    {
     "name": "Register user",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/register",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "register"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"{{authEmail}}\",\n  \"password\": \"correct-horse-battery-staple\",\n  \"merchantId\": \"{{merchantId}}\"\n}"
      },
      "description": "Attaches the user to the merchant registered above, granting MERCHANT_ADMIN scoped to it. That scope is what every request in 'Authenticated access' relies on."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('authEmail', 'owner+' + Date.now() + '@paymesh.example');"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('opaque prefixed id', () => pm.expect(b.id).to.match(/^usr_[0-9a-f-]{36}$/));",
         "pm.test('starts active', () => pm.expect(b.status).to.eql('ACTIVE'));",
         "pm.test('never echoes a password', () => pm.expect(JSON.stringify(b)).to.not.include('correct-horse'));",
         "pm.test('scoped to the merchant just created', () => pm.expect(b.roles[0].merchantId).to.eql(pm.collectionVariables.get('merchantId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Login",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/login",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "login"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"{{authEmail}}\",\n  \"password\": \"correct-horse-battery-staple\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('bearer token type', () => pm.expect(b.tokenType).to.eql('Bearer'));",
         "pm.test('access token is a three-part JWT', () => pm.expect(b.accessToken.split('.')).to.have.lengthOf(3));",
         "pm.test('short-lived access token', () => pm.expect(b.expiresIn).to.eql(900));",
         "pm.test('refresh token is opaque, not a JWT', () => pm.expect(b.refreshToken.split('.')).to.have.lengthOf(1));",
         "pm.collectionVariables.set('accessToken', b.accessToken);",
         "pm.collectionVariables.set('refreshToken', b.refreshToken);",
         "pm.collectionVariables.set('firstRefreshToken', b.refreshToken);"
        ]
       }
      }
     ]
    },
    {
     "name": "Login - wrong password (401)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/login",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "login"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"{{authEmail}}\",\n  \"password\": \"definitely-not-the-password\"\n}"
      },
      "description": "The body here must be byte-identical to the unknown-email response below: a difference would let an attacker enumerate registered addresses."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('INVALID_CREDENTIALS'));",
         "pm.collectionVariables.set('wrongPasswordBody', pm.response.text());"
        ]
       }
      }
     ]
    },
    {
     "name": "Login - unknown email (401, identical body)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/login",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "login"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"nobody-here@paymesh.example\",\n  \"password\": \"definitely-not-the-password\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('response is indistinguishable from a wrong password', () => pm.expect(pm.response.text()).to.eql(pm.collectionVariables.get('wrongPasswordBody')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Register - duplicate email (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/register",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "register"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"{{authEmail}}\",\n  \"password\": \"correct-horse-battery-staple\",\n  \"merchantId\": \"{{merchantId}}\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('USER_EMAIL_ALREADY_EXISTS'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refresh token (rotates)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/token/refresh",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "token",
        "refresh"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"refreshToken\": \"{{refreshToken}}\"\n}"
      },
      "description": "Spending a refresh token revokes it and mints a successor in the same family."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('a new refresh token was issued', () => pm.expect(b.refreshToken).to.not.eql(pm.collectionVariables.get('refreshToken')));",
         "pm.collectionVariables.set('refreshToken', b.refreshToken);"
        ]
       }
      }
     ]
    },
    {
     "name": "Refresh - reuse a spent token (401, revokes the family)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/token/refresh",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "token",
        "refresh"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"refreshToken\": \"{{firstRefreshToken}}\"\n}"
      },
      "description": "Presenting an already-spent token means it leaked. There is no way to tell the thief from the victim, so the entire family is revoked and both are forced back through login. The next request proves the live successor died with it."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('INVALID_REFRESH_TOKEN'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refresh - successor is dead too (401)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/token/refresh",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "token",
        "refresh"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"refreshToken\": \"{{refreshToken}}\"\n}"
      },
      "description": "This token was valid a moment ago. Reuse detection revoked the whole lineage."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('INVALID_REFRESH_TOKEN'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Logout",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/logout",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "logout"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"refreshToken\": \"{{refreshToken}}\"\n}"
      },
      "description": "Idempotent: an unknown or already-revoked token is a silent no-op, so logout cannot be used to probe whether a token is still live."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('204 No Content', () => pm.response.to.have.status(204));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Authenticated access",
   "description": "Everything that requires a bearer token. Run 'Merchant (public onboarding)' and 'Identity & Auth' first -- these use the access token captured by Login.\n\nThe cross-tenant requests build a second, unrelated merchant and user to prove that one tenant cannot see another's data.",
   "item": [
    {
     "name": "Get merchant - no token (401)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants/{{merchantId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants",
        "{{merchantId}}"
       ]
      },
      "description": "Reading a merchant is not public, even though creating one is."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('UNAUTHENTICATED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Get merchant - forged token (401)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants/{{merchantId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants",
        "{{merchantId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{forgedToken}}"
       }
      ],
      "description": "A structurally valid JWT carrying the right claims but signed with the wrong key. The filter chain verifies the signature, so claims alone buy nothing."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));"
        ]
       }
      }
     ]
    },
    {
     "name": "Get merchant - own (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants/{{merchantId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants",
        "{{merchantId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('is the caller\\'s merchant', () => pm.expect(pm.response.json().id).to.eql(pm.collectionVariables.get('merchantId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create customer",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/customers",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "customers"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"Buyer@Example.Test\",\n  \"merchantReference\": \"  order-42  \",\n  \"name\": \"  Jamie Buyer  \"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "There is no merchantId field in this body. The tenant comes from the token, which is what makes writing under someone else's merchant impossible rather than merely discouraged."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('opaque prefixed id', () => pm.expect(b.id).to.match(/^cus_[0-9a-f-]{36}$/));",
         "pm.test('tenant taken from the token', () => pm.expect(b.merchantId).to.eql(pm.collectionVariables.get('merchantId')));",
         "pm.test('email lowercased', () => pm.expect(b.email).to.eql('buyer@example.test'));",
         "pm.test('reference trimmed', () => pm.expect(b.merchantReference).to.eql('order-42'));",
         "pm.test('lookup hashes are never exposed', () => { const s = JSON.stringify(b); pm.expect(s).to.not.include('emailHash'); pm.expect(s).to.not.include('phoneHash'); });",
         "pm.collectionVariables.set('customerId', b.id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Get customer - own (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/customers/{{customerId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "customers",
        "{{customerId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('same customer', () => pm.expect(pm.response.json().id).to.eql(pm.collectionVariables.get('customerId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create customer - duplicate reference (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/customers",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "customers"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"another@example.test\",\n  \"merchantReference\": \"order-42\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Unique per merchant, not globally."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('CUSTOMER_REFERENCE_ALREADY_EXISTS'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create customer - no token (401)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/customers",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "customers"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"nobody@example.test\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create customer - invalid email (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/customers",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "customers"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"not-an-email\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('VALIDATION_FAILED'));",
         "pm.test('names the field', () => pm.expect(pm.response.json().fieldErrors.email).to.exist);"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant setup: register a second merchant",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"businessName\": \"Unrelated Business\",\n  \"email\": \"{{otherEmail}}\",\n  \"country\": \"IN\",\n  \"defaultCurrency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('otherEmail', 'other+' + Date.now() + '@paymesh.example');"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('otherMerchantId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant setup: activate the second merchant (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants/{{otherMerchantId}}/activate",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants",
        "{{otherMerchantId}}",
        "activate"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{otherPlatformAdminToken}}"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"KYC cleared in the sandbox\"\n}"
      },
      "description": "The second merchant needs activating for the same reason the first did (ADR-021), and forgetting it produces a subtler failure than forgetting the first.\n\nEvery cross-tenant test below asserts that reaching into another tenant answers 404 or 422 -- revealing nothing about whether the row exists. Against a PENDING_VERIFICATION merchant those requests get 403 MERCHANT_NOT_ACTIVE instead, which LOOKS like a passing isolation test if you only glance at the status class. It is not: the request was refused for the wrong reason, and the tenant boundary was never actually exercised.\n\nSee the first activation request for where the token comes from and why minting it is acceptable on the dev profile."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// Same mint as the first activation, scoped to the second merchant. PLATFORM_ADMIN authority is",
         "// platform-wide and carries no merchant at all since ADR-027 -- but the two mints stay separate",
         "// so each token tells its own story in a log.",
         "const secret = 'dev-only-insecure-jwt-signing-secret-change-me';",
         "const now = Math.floor(Date.now() / 1000);",
         "",
         "const b64url = (w) => CryptoJS.enc.Base64.stringify(w)",
         "    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');",
         "const seg = (o) => b64url(CryptoJS.enc.Utf8.parse(JSON.stringify(o)));",
         "",
         "const header = seg({ alg: 'HS256', typ: 'JWT' });",
         "const payload = seg({",
         "    iss: 'paymesh',",
         "    sub: 'usr_00000000-0000-0000-0000-0000000000ad',",
         "    iat: now,",
         "    exp: now + 900,",
         "    email: 'platform-admin@paymesh.test',",
         "    roles: ['PLATFORM_ADMIN']",
         "});",
         "",
         "const signature = b64url(CryptoJS.HmacSHA256(header + '.' + payload, secret));",
         "pm.collectionVariables.set('otherPlatformAdminToken', header + '.' + payload + '.' + signature);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('ACTIVE -- so cross-tenant refusals below are about TENANCY, not status', () =>",
         "    pm.expect(pm.response.json().status).to.eql('ACTIVE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant setup: register its user",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/register",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "register"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"{{otherUserEmail}}\",\n  \"password\": \"correct-horse-battery-staple\",\n  \"merchantId\": \"{{otherMerchantId}}\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('otherUserEmail', 'otheruser+' + Date.now() + '@paymesh.example');"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant setup: log it in",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/auth/login",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "auth",
        "login"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"{{otherUserEmail}}\",\n  \"password\": \"correct-horse-battery-staple\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.collectionVariables.set('otherAccessToken', pm.response.json().accessToken);"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant: read another merchant (404, not 403)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/merchants/{{merchantId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "merchants",
        "{{merchantId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ],
      "description": "Answering 403 would confirm the id is real, turning this endpoint into an oracle for enumerating tenants. It is reported as not found instead."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('MERCHANT_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant: read another merchant's customer (404)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/customers/{{customerId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "customers",
        "{{customerId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ],
      "description": "Same rule one level down: an id alone never authorizes a read."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('CUSTOMER_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant: same merchantReference is free (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/customers",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "customers"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"buyer@unrelated.test\",\n  \"merchantReference\": \"order-42\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ],
      "description": "The reference that just returned 409 for the first merchant is available here, because uniqueness is scoped to the tenant."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.test('written under the second merchant', () => pm.expect(pm.response.json().merchantId).to.eql(pm.collectionVariables.get('otherMerchantId')));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Orders",
   "description": "Runs after Authenticated access, which captured accessToken, customerId and otherAccessToken. Covers both writes behind the idempotency layer, cursor pagination, and the cross-tenant 404.",
   "item": [
    {
     "name": "Setup: a customer under the second merchant",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/customers",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "customers"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"email\": \"other-buyer@example.test\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ],
      "description": "Belongs to the SECOND merchant. Used below to prove that naming another tenant's customer on an order is rejected without confirming the customer is real."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('otherCustomerId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"merchantOrderReference\": \"  ORDER-7788  \",\n  \"amountMinor\": 1999,\n  \"currency\": \"inr\",\n  \"description\": \"Two masala chai\",\n  \"metadata\": { \"channel\": \"web\" }\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{createOrderKey}}"
       }
      ],
      "description": "There is no merchantId in this body. The tenant comes from the token. Money is an integer count of minor units with the currency in its own field."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('createOrderKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('opaque prefixed id', () => pm.expect(b.id).to.match(/^ord_[0-9a-f-]{36}$/));",
         "pm.test('tenant taken from the token', () => pm.expect(b.merchantId).to.eql(pm.collectionVariables.get('merchantId')));",
         "pm.test('amount is an integer of minor units', () => { pm.expect(b.amountMinor).to.eql(1999); pm.expect(Number.isInteger(b.amountMinor)).to.be.true; });",
         "pm.test('currency uppercased', () => pm.expect(b.currency).to.eql('INR'));",
         "pm.test('reference trimmed', () => pm.expect(b.merchantOrderReference).to.eql('ORDER-7788'));",
         "pm.test('nothing paid yet', () => pm.expect(b.amountPaidMinor).to.eql(0));",
         "pm.test('starts PENDING', () => pm.expect(b.status).to.eql('PENDING'));",
         "pm.test('metadata echoed back', () => pm.expect(b.metadata.channel).to.eql('web'));",
         "pm.test('not cancelled', () => pm.expect(b.cancelledAt).to.be.oneOf([null, undefined]));",
         "pm.test('no row version leaked', () => pm.expect(JSON.stringify(b)).to.not.include('version'));",
         "pm.collectionVariables.set('orderId', b.id);",
         "pm.collectionVariables.set('firstOrderBody', pm.response.text());"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order - replay the same key and body (idempotent)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"merchantOrderReference\": \"  ORDER-7788  \",\n  \"amountMinor\": 1999,\n  \"currency\": \"inr\",\n  \"description\": \"Two masala chai\",\n  \"metadata\": { \"channel\": \"web\" }\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{createOrderKey}}"
       }
      ],
      "description": "A network retry. Same key, same body: the stored response comes back verbatim and the handler never runs again."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created, replayed not re-executed', () => pm.response.to.have.status(201));",
         "pm.test('marked as a replay', () => pm.expect(pm.response.headers.get('Idempotency-Replayed')).to.eql('true'));",
         "pm.test('byte-identical to the original response', () => pm.expect(pm.response.text()).to.eql(pm.collectionVariables.get('firstOrderBody')));",
         "pm.test('no second order was created', () => pm.expect(pm.response.json().id).to.eql(pm.collectionVariables.get('orderId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order - same key, different body (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 9999,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{createOrderKey}}"
       }
      ],
      "description": "The key was already used for a different request. Replaying the first answer would be a lie, so this is refused rather than served from the record."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('IDEMPOTENCY_KEY_REUSED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order - no Idempotency-Key (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The header is mandatory on this route: a write that can create money movement must be safe to retry."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('IDEMPOTENCY_KEY_REQUIRED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order - duplicate merchantOrderReference (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"merchantOrderReference\": \"ORDER-7788\",\n  \"amountMinor\": 500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{dupRefKey}}"
       }
      ],
      "description": "A FRESH Idempotency-Key, so this is a genuinely new request -- and it still hits the unique constraint. The two de-duplication rules are independent: the header answers 'same request?', the reference answers 'same purchase?'."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('dupRefKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('ORDER_REFERENCE_ALREADY_EXISTS'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order - zero amount (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 0,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{zeroAmountKey}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('zeroAmountKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('VALIDATION_FAILED'));",
         "pm.test('names the offending field', () => pm.expect(pm.response.json().fieldErrors).to.have.property('amountMinor'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order - linked to a customer of this merchant (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"customerId\": \"{{customerId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{linkedOrderKey}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('linkedOrderKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.test('customer linked', () => pm.expect(pm.response.json().customerId).to.eql(pm.collectionVariables.get('customerId')));",
         "pm.collectionVariables.set('linkedOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order - customer of another merchant (422, reveals nothing)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"customerId\": \"{{otherCustomerId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{strangerCustomerKey}}"
       }
      ],
      "description": "The customer is real; it belongs to the other merchant. The next request proves the answer is identical to one for a customer that never existed, so this endpoint cannot be used to confirm another tenant's customer ids."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('strangerCustomerKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Content', () => pm.response.to.have.status(422));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('CUSTOMER_NOT_FOUND'));",
         "pm.collectionVariables.set('strangerCustomerBody', pm.response.text().replace(pm.collectionVariables.get('otherCustomerId'), 'X'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order - customer that never existed (422, identical answer)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"customerId\": \"{{ghostCustomerId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{ghostCustomerKey}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('ghostCustomerKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('ghostCustomerId', 'cus_' + pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Content', () => pm.response.to.have.status(422));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('CUSTOMER_NOT_FOUND'));",
         "pm.test('indistinguishable from another tenant\\'s customer', () => pm.expect(pm.response.text().replace(pm.collectionVariables.get('ghostCustomerId'), 'X')).to.eql(pm.collectionVariables.get('strangerCustomerBody')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Get order - own (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{orderId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{orderId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('same order', () => pm.expect(pm.response.json().id).to.eql(pm.collectionVariables.get('orderId')));",
         "pm.test('still PENDING', () => pm.expect(pm.response.json().status).to.eql('PENDING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant: read another merchant's order (404, not 403)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{orderId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{orderId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ],
      "description": "A real order id and a valid token for a different merchant. 403 would confirm the id exists and turn this route into an oracle for enumerating other tenants' orders."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('not 403 -- existence must not leak', () => pm.expect(pm.response.code).to.not.eql(403));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('ORDER_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Get order - malformed id (400)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/not_an_order_id",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "not_an_order_id"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('INVALID_REQUEST'));"
        ]
       }
      }
     ]
    },
    {
     "name": "List orders (200, pagination envelope)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('collection envelope, not a bare array', () => { pm.expect(b).to.have.property('data'); pm.expect(b.data).to.be.an('array'); pm.expect(b).to.have.property('pagination'); });",
         "pm.test('default limit is 20', () => pm.expect(b.pagination.limit).to.eql(20));",
         "pm.test('pagination states hasMore', () => pm.expect(b.pagination).to.have.property('hasMore'));",
         "pm.test('every order belongs to the caller', () => b.data.forEach(o => pm.expect(o.merchantId).to.eql(pm.collectionVariables.get('merchantId'))));",
         "pm.test('the created orders are listed', () => pm.expect(b.data.map(o => o.id)).to.include(pm.collectionVariables.get('orderId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "List orders - first page of one (200, captures the cursor)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders?limit=1",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ],
       "query": [
        {
         "key": "limit",
         "value": "1"
        }
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('one row', () => pm.expect(b.data.length).to.eql(1));",
         "pm.test('more to come', () => pm.expect(b.pagination.hasMore).to.eql(true));",
         "pm.test('cursor is opaque', () => { pm.expect(b.pagination.nextCursor).to.be.a('string'); pm.expect(b.pagination.nextCursor).to.not.include('ord_'); });",
         "pm.collectionVariables.set('orderCursor', b.pagination.nextCursor);",
         "pm.collectionVariables.set('firstPageId', b.data[0].id);"
        ]
       }
      }
     ]
    },
    {
     "name": "List orders - second page via the cursor (200, no repeats)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders?limit=1&cursor={{orderCursor}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ],
       "query": [
        {
         "key": "limit",
         "value": "1"
        },
        {
         "key": "cursor",
         "value": "{{orderCursor}}"
        }
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Keyset pagination over (created_at, order_id). The order id is the tiebreak: without it, two orders created in the same instant would be skipped or repeated at a page boundary."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('one row', () => pm.expect(b.data.length).to.eql(1));",
         "pm.test('a different order than page one', () => pm.expect(b.data[0].id).to.not.eql(pm.collectionVariables.get('firstPageId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "List orders - limit above the cap (200, capped at 100)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders?limit=5000",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ],
       "query": [
        {
         "key": "limit",
         "value": "5000"
        }
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('capped, never unbounded', () => pm.expect(pm.response.json().pagination.limit).to.eql(100));"
        ]
       }
      }
     ]
    },
    {
     "name": "List orders - limit below one (400)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders?limit=0",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ],
       "query": [
        {
         "key": "limit",
         "value": "0"
        }
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('INVALID_REQUEST'));"
        ]
       }
      }
     ]
    },
    {
     "name": "List orders - unknown status filter (400)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders?status=NOPE",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ],
       "query": [
        {
         "key": "status",
         "value": "NOPE"
        }
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('INVALID_REQUEST'));",
         "pm.test('no internal type names in the message', () => pm.expect(pm.response.json().message).to.not.include('enum'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant: list orders sees none of them (200, empty)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('the other merchant has no orders', () => pm.expect(pm.response.json().data.length).to.eql(0));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant: cancel another merchant's order (404)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{orderId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{orderId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"not mine to cancel\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{crossCancelKey}}"
       }
      ],
      "description": "Cancelling is a write, so the tenant check matters more here than on a read. Still 404, not 403."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('crossCancelKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('ORDER_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel order (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{orderId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{orderId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"out of stock\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cancelKey}}"
       }
      ],
      "description": "Cancellation is requested as an action. There is no status field to set: the aggregate owns the state machine."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cancelKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('now CANCELLED', () => pm.expect(b.status).to.eql('CANCELLED'));",
         "pm.test('reason recorded', () => pm.expect(b.cancellationReason).to.eql('out of stock'));",
         "pm.test('cancellation is timestamped', () => pm.expect(b.cancelledAt).to.be.a('string'));",
         "pm.collectionVariables.set('cancelledBody', pm.response.text());"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel again on a FRESH key (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{orderId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{orderId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"out of stock\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{secondCancelKey}}"
       }
      ],
      "description": "A new key means a new request, so it reaches the state machine -- which refuses, because the order is no longer PENDING."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('secondCancelKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('ORDER_NOT_CANCELLABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel again on the SAME key (200, replayed)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{orderId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{orderId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"out of stock\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cancelKey}}"
       }
      ],
      "description": "The same request as the one that succeeded. It never reaches the state machine, so it gets the stored 200 rather than the 409 above."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('marked as a replay', () => pm.expect(pm.response.headers.get('Idempotency-Replayed')).to.eql('true'));",
         "pm.test('the original answer, verbatim', () => pm.expect(pm.response.text()).to.eql(pm.collectionVariables.get('cancelledBody')));"
        ]
       }
      }
     ]
    },
    {
     "name": "List orders - filter by status (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders?status=CANCELLED",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ],
       "query": [
        {
         "key": "status",
         "value": "CANCELLED"
        }
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('only cancelled orders', () => b.data.forEach(o => pm.expect(o.status).to.eql('CANCELLED')));",
         "pm.test('the cancelled order is there', () => pm.expect(b.data.map(o => o.id)).to.include(pm.collectionVariables.get('orderId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create order - no token (401)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{$guid}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('UNAUTHENTICATED'));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Outbox (order.created)",
   "description": "The outbox has no HTTP surface, and that is deliberate: outbox_events is internal platform state that a relay will read, never a resource a merchant can fetch. What can be asserted from outside is the boundary itself -- an order and its order.created event commit together, so an order that is readable proves its event was written, and a create that was refused proves neither row landed.\n\nThe rollback half of the invariant cannot be reached over HTTP (nothing makes the outbox insert fail on demand); it is proved in OutboxTransactionIntegrationTest.leavesNoOrderRowBehindWhenTheOutboxAppendFails. See ADR-010.\n\nRuns top to bottom and depends on the Identity & Auth folder having captured accessToken.",
   "item": [
    {
     "name": "Create an order (order + order.created, one transaction)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"merchantOrderReference\": \"OUTBOX-9001\",\n  \"amountMinor\": 4200,\n  \"currency\": \"INR\",\n  \"description\": \"An order that announces itself\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{outboxOrderKey}}"
       }
      ],
      "description": "One call, two rows: the order and its order.created event, in a single database transaction (ADR-010). Only the order is visible here."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('outboxOrderKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('opaque prefixed id', () => pm.expect(b.id).to.match(/^ord_[0-9a-f-]{36}$/));",
         "pm.test('starts PENDING', () => pm.expect(b.status).to.eql('PENDING'));",
         "pm.test('reference stored', () => pm.expect(b.merchantOrderReference).to.eql('OUTBOX-9001'));",
         "pm.test('the outbox stays internal: no event fields on the resource', () => {",
         "  ['eventId', 'eventType', 'outbox', 'payload', 'publishedAt', 'occurredAt']",
         "    .forEach(field => pm.expect(b).to.not.have.property(field));",
         "});",
         "pm.collectionVariables.set('outboxOrderId', b.id);"
        ]
       }
      }
     ]
    },
    {
     "name": "The order is readable, so its event committed with it (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{outboxOrderId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{outboxOrderId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The two writes share one commit, so a readable order is proof the event row is there too. If the outbox insert had failed, this would be a 404 -- the order would have rolled back with it."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('the same order came back', () => pm.expect(b.id).to.eql(pm.collectionVariables.get('outboxOrderId')));",
         "pm.test('amount survived the commit intact', () => pm.expect(b.amountMinor).to.eql(4200));",
         "pm.test('still PENDING: nothing consumes order.created yet', () => pm.expect(b.status).to.eql('PENDING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "A refused create announces nothing (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"merchantOrderReference\": \"OUTBOX-9001\",\n  \"amountMinor\": 4200,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{outboxDuplicateKey}}"
       }
      ],
      "description": "A FRESH Idempotency-Key, so this is a genuinely new request. It is refused before the transaction opens, so neither an order row nor an event row is written -- the next request counts what actually landed."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('outboxDuplicateKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('ORDER_REFERENCE_ALREADY_EXISTS'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Exactly one order carries the reference (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders?limit=100",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ],
       "query": [
        {
         "key": "limit",
         "value": "100"
        }
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The observable half of 'both or neither': the refused create left no partial order behind, so the reference appears exactly once."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const matches = pm.response.json().data.filter(o => o.merchantOrderReference === 'OUTBOX-9001');",
         "pm.test('one order, not two, not zero', () => pm.expect(matches).to.have.lengthOf(1));",
         "pm.test('and it is the one that was created', () => pm.expect(matches[0].id).to.eql(pm.collectionVariables.get('outboxOrderId')));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Payment Intents",
   "description": "Runs after Orders, which captured accessToken, customerId, otherAccessToken and linkedOrderId -- a 2500 INR order of the first merchant that is still PENDING.\n\nAn order says what is owed; an intent says how it is being collected. The intent is operational state, not the financial record: nothing here moves a balance, and the Ledger that will is not built.\n\nThe subject of this folder is ADR-011. An order holds AT MOST ONE live intent, and it is a partial unique index that enforces it -- the application's pre-check exists only for the friendlier message and is never trusted. Cancelling is how the slot is released.",
   "item": [
    {
     "name": "Create payment intent (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{linkedOrderId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"inr\",\n  \"description\": \"Collecting order 2500\",\n  \"metadata\": {\n    \"channel\": \"web\"\n  }\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{createIntentKey}}"
       }
      ],
      "description": "No merchantId and no status in the body: the tenant comes from the token and the state machine owns the state. The amount must be the order's EXACTLY -- one live intent for the whole obligation is what makes overpayment structurally impossible rather than merely check-constrained."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('createIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('opaque prefixed id', () => pm.expect(b.id).to.match(/^pi_[0-9a-f-]{36}$/));",
         "pm.test('tenant taken from the token', () => pm.expect(b.merchantId).to.eql(pm.collectionVariables.get('merchantId')));",
         "pm.test('collects the order it names', () => pm.expect(b.orderId).to.eql(pm.collectionVariables.get('linkedOrderId')));",
         "pm.test('customer copied from the order', () => pm.expect(b.customerId).to.eql(pm.collectionVariables.get('customerId')));",
         "pm.test('amount is the order amount, in minor units', () => { pm.expect(b.amountMinor).to.eql(2500); pm.expect(Number.isInteger(b.amountMinor)).to.be.true; });",
         "pm.test('currency uppercased', () => pm.expect(b.currency).to.eql('INR'));",
         "pm.test('starts REQUIRES_PAYMENT_METHOD', () => pm.expect(b.status).to.eql('REQUIRES_PAYMENT_METHOD'));",
         "pm.test('captureMethod defaults to AUTOMATIC', () => pm.expect(b.captureMethod).to.eql('AUTOMATIC'));",
         "pm.test('nothing captured or refunded', () => { pm.expect(b.capturedAmountMinor).to.eql(0); pm.expect(b.refundedAmountMinor).to.eql(0); });",
         "pm.test('not cancelled', () => { pm.expect(b.cancelledAt).to.be.oneOf([null, undefined]); pm.expect(b.cancellationReason).to.be.oneOf([null, undefined]); });",
         "pm.test('metadata echoed back', () => pm.expect(b.metadata.channel).to.eql('web'));",
         "pm.test('NO clientSecret -- a credential nothing can verify is not issued', () => { pm.expect(b).to.not.have.property('clientSecret'); pm.expect(pm.response.text()).to.not.include('clientSecret'); });",
         "pm.test('no row version leaked', () => pm.expect(b).to.not.have.property('version'));",
         "pm.collectionVariables.set('paymentIntentId', b.id);",
         "pm.collectionVariables.set('firstIntentBody', pm.response.text());"
        ]
       }
      }
     ]
    },
    {
     "name": "Create a second intent for the same order (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{linkedOrderId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{secondIntentKey}}"
       }
      ],
      "description": "A FRESH Idempotency-Key, so this is a genuinely new request and reaches the create path. The order already holds a live intent and uq_payment_intents_live_per_order says it may hold only one (ADR-011). Retrying this will never succeed: the way forward is to cancel the live intent, which is what the last requests in this folder do."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('secondIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('ORDER_HAS_ACTIVE_PAYMENT_INTENT'));",
         "pm.test('no second intent was created', () => pm.expect(pm.response.json()).to.not.have.property('id'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create - amount is not the order's (422)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{linkedOrderId}}\",\n  \"amountMinor\": 2400,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{mismatchAmountKey}}"
       }
      ],
      "description": "422, not 400: nothing about the request is malformed, the amount simply is not the order's. An intent collects its order's exact obligation, so there is no partial and no over-collection to argue about."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('mismatchAmountKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Content', () => pm.response.to.have.status(422));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_AMOUNT_MISMATCH'));",
         "pm.test('checked before the live-intent slot, so this is not reported as a 409', () => pm.expect(pm.response.code).to.not.eql(409));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant: create against another merchant's order (422)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{linkedOrderId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{crossTenantIntentKey}}"
       }
      ],
      "description": "A real order id and a valid token for a different merchant. ORDER_NOT_PAYABLE is ONE code for three causes -- no such order, not this merchant's order, order not PENDING -- because splitting them would turn this endpoint into an oracle for enumerating another tenant's order ids."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('crossTenantIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Content', () => pm.response.to.have.status(422));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('ORDER_NOT_PAYABLE'));",
         "pm.test('not a 409 -- the other merchant is not told the order has a live intent', () => pm.expect(pm.response.code).to.not.eql(409));",
         "pm.test('no intent came back', () => pm.expect(pm.response.json()).to.not.have.property('id'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Get payment intent - own (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{paymentIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{paymentIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('the same resource create returned', () => pm.expect(b).to.eql(JSON.parse(pm.collectionVariables.get('firstIntentBody'))));",
         "pm.test('id', () => pm.expect(b.id).to.eql(pm.collectionVariables.get('paymentIntentId')));",
         "pm.test('still REQUIRES_PAYMENT_METHOD', () => pm.expect(b.status).to.eql('REQUIRES_PAYMENT_METHOD'));",
         "pm.test('amount and currency unchanged since creation', () => { pm.expect(b.amountMinor).to.eql(2500); pm.expect(b.currency).to.eql('INR'); });",
         "pm.test('still no clientSecret on a read either', () => pm.expect(b).to.not.have.property('clientSecret'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant: read another merchant's intent (404, not 403)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{paymentIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{paymentIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ],
      "description": "A pi_ in a path authorizes nothing. 403 would confirm the intent exists; 404 says only that this merchant has no such intent."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('not 403 -- existence must not leak', () => pm.expect(pm.response.code).to.not.eql(403));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "List payment intents (200, pagination envelope)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('collection envelope, not a bare array', () => { pm.expect(b).to.have.property('data'); pm.expect(b.data).to.be.an('array'); pm.expect(b).to.have.property('pagination'); });",
         "pm.test('default limit is 20', () => pm.expect(b.pagination.limit).to.eql(20));",
         "pm.test('pagination states hasMore', () => pm.expect(b.pagination).to.have.property('hasMore'));",
         "pm.test('every intent belongs to the caller', () => b.data.forEach(i => pm.expect(i.merchantId).to.eql(pm.collectionVariables.get('merchantId'))));",
         "pm.test('the created intent is listed', () => pm.expect(b.data.map(i => i.id)).to.include(pm.collectionVariables.get('paymentIntentId')));",
         "pm.test('the refused creates left nothing behind', () => pm.expect(b.data.filter(i => i.orderId === pm.collectionVariables.get('linkedOrderId'))).to.have.lengthOf(1));"
        ]
       }
      }
     ]
    },
    {
     "name": "List - filter by orderId (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents?orderId={{linkedOrderId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ],
       "query": [
        {
         "key": "orderId",
         "value": "{{linkedOrderId}}"
        }
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Which intents does this order have? Exactly one, and that is ADR-011 observable."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('only this order\\'s intents', () => b.data.forEach(i => pm.expect(i.orderId).to.eql(pm.collectionVariables.get('linkedOrderId'))));",
         "pm.test('the created intent is there', () => pm.expect(b.data.map(i => i.id)).to.include(pm.collectionVariables.get('paymentIntentId')));",
         "pm.test('one live intent per order', () => pm.expect(b.data).to.have.lengthOf(1));"
        ]
       }
      }
     ]
    },
    {
     "name": "List - filter by a status nothing has reached (200, empty)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents?status=SUCCEEDED",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ],
       "query": [
        {
         "key": "status",
         "value": "SUCCEEDED"
        }
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "SUCCEEDED is declared in the status CHECK from V8 but no transition reaches it yet -- the provider callbacks that would are a later PR. An empty data array is the whole point: the filter is honoured, and the state is genuinely unreachable today."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('a known status is accepted, not rejected', () => pm.expect(pm.response.code).to.not.eql(400));",
         "pm.test('data is empty', () => { pm.expect(b.data).to.be.an('array'); pm.expect(b.data).to.have.lengthOf(0); });",
         "pm.test('the envelope is still an envelope', () => { pm.expect(b.pagination.hasMore).to.eql(false); pm.expect(b.pagination.nextCursor).to.be.oneOf([null, undefined]); });"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel payment intent (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{paymentIntentId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{paymentIntentId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"customer changed their mind\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cancelIntentKey}}"
       }
      ],
      "description": "Cancellation is requested as an action; there is no status field to set. It is also the only way the order's live-intent slot is released (ADR-011), which is why every state a customer can strand an intent in has a merchant cancel route -- except PROCESSING, deliberately, because an in-flight attempt may already have succeeded at the provider."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cancelIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('now CANCELLED', () => pm.expect(b.status).to.eql('CANCELLED'));",
         "pm.test('reason recorded', () => pm.expect(b.cancellationReason).to.eql('customer changed their mind'));",
         "pm.test('cancellation is timestamped', () => pm.expect(b.cancelledAt).to.be.a('string'));",
         "pm.test('same intent, not a new one', () => pm.expect(b.id).to.eql(pm.collectionVariables.get('paymentIntentId')));",
         "pm.test('cancelling collects nothing', () => pm.expect(b.capturedAmountMinor).to.eql(0));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel again on a FRESH key (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{paymentIntentId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{paymentIntentId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"customer changed their mind\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{secondCancelIntentKey}}"
       }
      ],
      "description": "A new key means a new request, so it reaches the state machine -- which refuses, because CANCELLED is terminal."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('secondCancelIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_CANCELLABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Create payment intent - no Idempotency-Key (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{linkedOrderId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The header is mandatory on this route. Rejected by the idempotency filter before the handler runs, so nothing is created -- note the order's slot is free at this point and this create would otherwise have succeeded."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('IDEMPOTENCY_KEY_REQUIRED'));",
         "pm.test('refused before the handler, so no intent came back', () => pm.expect(pm.response.json()).to.not.have.property('id'));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Payment Intents - attach, confirm and the ADR-013 guard",
   "item": [
    {
     "name": "Setup: an order to attach and confirm against (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{confirmOrderKey}}"
       }
      ],
      "description": "Its own order, because the intents earlier in this collection have already been cancelled and an order holds at most one live intent (ADR-011)."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('confirmOrderKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('confirmOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a payment intent for it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{confirmOrderId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{confirmIntentKey}}"
       }
      ],
      "description": "Starts at REQUIRES_PAYMENT_METHOD with no payment method: paymentMethodType is null until one is attached, and nothing defaults it."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('confirmIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('starts REQUIRES_PAYMENT_METHOD', () => pm.expect(b.status).to.eql('REQUIRES_PAYMENT_METHOD'));",
         "pm.test('no method until one is attached', () => pm.expect(b.paymentMethodType).to.be.oneOf([null, undefined]));",
         "pm.collectionVariables.set('confirmIntentId', b.id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Confirm before attaching (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{earlyConfirmKey}}"
       }
      ],
      "description": "Attach is a genuine prerequisite, not a convention. An intent with no instrument has nothing to be collected with, and inferring a default would pick one on the merchant's behalf."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('earlyConfirmKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code names the state machine', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_CONFIRMABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Attach a payment method (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{attachKey}}"
       }
      ],
      "description": "A method TYPE, not a token. payment_method_tokens exists (V3) and nothing can write to it until the Provider Simulator does, so requiring a token would make this endpoint uncallable. No PAN, no expiry, no holder name reaches PayMesh (SDD 12.6)."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('attachKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('now REQUIRES_CONFIRMATION', () => pm.expect(b.status).to.eql('REQUIRES_CONFIRMATION'));",
         "pm.test('the type is recorded', () => pm.expect(b.paymentMethodType).to.eql('CARD'));",
         "pm.test('attaching collects nothing', () => pm.expect(b.capturedAmountMinor).to.eql(0));",
         "pm.test('no instrument data echoed back', () => { const t = pm.response.text(); pm.expect(t).to.not.include('token'); pm.expect(t).to.not.include('pan'); });",
         "pm.collectionVariables.set('attachedBody', pm.response.text());"
        ]
       }
      }
     ]
    },
    {
     "name": "Attach again on the SAME key (200, replayed)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{attachKey}}"
       }
      ],
      "description": "The same key never reaches the state machine at all: the stored response comes back, byte for byte, with Idempotency-Replayed: true."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('replayed, not re-executed', () => pm.expect(pm.response.headers.get('Idempotency-Replayed')).to.eql('true'));",
         "pm.test('identical body', () => pm.expect(pm.response.text()).to.eql(pm.collectionVariables.get('attachedBody')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Attach again on a FRESH key (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"UPI\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{reattachKey}}"
       }
      ],
      "description": "A genuinely new request, so it meets the state machine. Re-attaching is refused rather than quietly allowed: past this point an attempt may be in flight, and a stored method that disagrees with what the provider was asked for is worse than none. Cancel and start again."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('reattachKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_METHOD_NOT_ATTACHABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Attach an unknown method type (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CRYPTO\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{badMethodKey}}"
       }
      ],
      "description": "CARD, UPI, NET_BANKING or WALLET. Refused at the boundary, and the error names the field rather than a Java type."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('badMethodKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "const b = pm.response.json();",
         "pm.test('code', () => pm.expect(b.code).to.eql('VALIDATION_FAILED'));",
         "pm.test('names the field', () => pm.expect(b.fieldErrors).to.have.property('paymentMethodType'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Confirm the payment intent (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"returnUrl\": \"https://shop.example/checkout/return?session=SECRET\",\n  \"device\": \"web\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{confirmKey}}"
       }
      ],
      "description": "202 AND NOT 200, even though nothing asynchronous is invoked: the intent is PROCESSING and the outcome is undecided until a provider callback resolves it. There is no outbound call anywhere in Payment yet. The returnUrl's query string is stripped before it is stored on the attempt -- it routinely carries a session token and that row is a durable audit record."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('confirmKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted -- the work is started, not finished', () => pm.response.to.have.status(202));",
         "const b = pm.response.json();",
         "pm.test('now PROCESSING', () => pm.expect(b.status).to.eql('PROCESSING'));",
         "pm.test('the method survives the transition', () => pm.expect(b.paymentMethodType).to.eql('CARD'));",
         "pm.test('confirming captures nothing -- the Ledger is not involved', () => { pm.expect(b.capturedAmountMinor).to.eql(0); pm.expect(b.refundedAmountMinor).to.eql(0); });",
         "pm.test('same intent, not a new one', () => pm.expect(b.id).to.eql(pm.collectionVariables.get('confirmIntentId')));",
         "pm.collectionVariables.set('confirmedBody', pm.response.text());"
        ]
       }
      }
     ]
    },
    {
     "name": "Confirm again on the SAME key (202, replayed)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"returnUrl\": \"https://shop.example/checkout/return?session=SECRET\",\n  \"device\": \"web\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{confirmKey}}"
       }
      ],
      "description": "A network retry must cost nothing -- and must not open a second attempt against the same intent."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.test('replayed, not re-executed', () => pm.expect(pm.response.headers.get('Idempotency-Replayed')).to.eql('true'));",
         "pm.test('identical body', () => pm.expect(pm.response.text()).to.eql(pm.collectionVariables.get('confirmedBody')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Confirm again on a FRESH key (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{reconfirmKey}}"
       }
      ],
      "description": "A genuinely new request meets the state machine, which refuses it: PROCESSING is not confirmable."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('reconfirmKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_CONFIRMABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel a PROCESSING intent (409, deliberately)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"too late\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cancelProcessingKey}}"
       }
      ],
      "description": "THE ONE STATE WITH NO CANCEL, and it is a named deferral rather than an oversight (ADR-011 section 5). An in-flight attempt may already have succeeded at the provider, so cancelling locally could erase a payment that really happened -- money taken from a customer with no record of it here, which is strictly worse than a stuck order. The full answer is a PROCESSING timeout plus provider reconciliation, and neither is in scope."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cancelProcessingKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_CANCELLABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a second order (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{guardOrderKey}}"
       }
      ],
      "description": "For the ADR-013 walkthrough below: an order that will be cancelled out from under a live intent."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('guardOrderKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('guardOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: an intent for it, with a method attached (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{guardOrderId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{guardIntentKey}}"
       }
      ],
      "description": "Created while the order was payable, which is the only time Payment used to check."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('guardIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('guardIntentId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach a method to it (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{guardIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{guardIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"UPI\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{guardAttachKey}}"
       }
      ],
      "description": "Attach is not guarded by the order re-read: it commits PayMesh to nothing."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('guardAttachKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('now REQUIRES_CONFIRMATION', () => pm.expect(pm.response.json().status).to.eql('REQUIRES_CONFIRMATION'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel the ORDER out from under the live intent (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{guardOrderId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{guardOrderId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"sold out\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{guardOrderCancelKey}}"
       }
      ],
      "description": "Nothing stops this, and nothing should: Order.cancel checks only that the order is PENDING and must never learn that Payment exists (design section 0.5). The order is now CANCELLED with a live intent still attached to it."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('guardOrderCancelKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('order is CANCELLED', () => pm.expect(pm.response.json().status).to.eql('CANCELLED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Confirm against the cancelled order (422)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{guardIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{guardIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{guardConfirmKey}}"
       }
      ],
      "description": "ADR-013, AND THE MOST IMPORTANT REQUEST IN THIS FOLDER. Confirm re-reads the order's payability inside its own transaction and refuses. Without it PayMesh would collect for an order the merchant explicitly cancelled. ORDER_NOT_PAYABLE is one code for three causes -- no such order, not yours, not payable -- because splitting them would let a caller enumerate another tenant's order ids."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('guardConfirmKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Entity', () => pm.response.to.have.status(422));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('ORDER_NOT_PAYABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel the intent awaiting confirmation (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{guardIntentId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{guardIntentId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"order was cancelled\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{guardIntentCancelKey}}"
       }
      ],
      "description": "The route out of REQUIRES_CONFIRMATION, new in this PR (ADR-011's slot-release table). Without it an abandoned checkout would hold the order's only slot forever, and a stuck intent is a stuck order. The method it died holding is kept, not cleared."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('guardIntentCancelKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('now CANCELLED', () => pm.expect(b.status).to.eql('CANCELLED'));",
         "pm.test('the method it died holding is kept', () => pm.expect(b.paymentMethodType).to.eql('UPI'));",
         "pm.test('cancellation is timestamped', () => pm.expect(b.cancelledAt).to.be.a('string'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Attach with no Idempotency-Key (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Both new writes are registered in IdempotencyConfiguration.IDEMPOTENT_ROUTES as path templates, so both require the header."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('IDEMPOTENCY_KEY_REQUIRED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Confirm with no Idempotency-Key (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{confirmIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{confirmIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Same registry, same answer."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('code', () => pm.expect(pm.response.json().code).to.eql('IDEMPOTENCY_KEY_REQUIRED'));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Provider callbacks (internal, HMAC-signed)",
   "description": "POST /internal/v1/provider-callbacks/{provider}. NOT the merchant surface: no bearer token, not under /api/, and deliberately not idempotency-keyed -- provider_callbacks is its deduplication (ADR-012). Every request here signs its own body in a pre-request script; there is no fixed signature to paste, because a signature is valid for one body at one second.",
   "item": [
    {
     "name": "Setup: an order to collect (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbOrderKey}}"
       }
      ],
      "description": "Its own order: an order holds at most one live payment intent (ADR-011), and every intent earlier in this collection has already claimed one."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbOrderKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('providerCallbackSecret', 'dev-only-insecure-provider-callback-secret-change-me');",
         "pm.collectionVariables.set('cbReference', 'sim_pay_' + pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('cbOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a payment intent for it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{cbOrderId}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbIntentKey}}"
       }
      ],
      "description": "REQUIRES_PAYMENT_METHOD. Nothing a provider says can reach it yet."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('cbIntentId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach a payment method (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{cbIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{cbIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbAttachKey}}"
       }
      ],
      "description": "REQUIRES_CONFIRMATION."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbAttachKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: confirm it (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{cbIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{cbIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbConfirmKey}}"
       }
      ],
      "description": "PROCESSING, with one attempt open. 202 because the outcome is genuinely undecided until a callback resolves it -- which is what the rest of this folder does."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbConfirmKey', pm.variables.replaceIn('{{$guid}}'));",
         "// A fixed base instant for the ordering demonstrations below, so 'earlier' and 'later' are",
         "// facts about the provider's clock rather than about how fast this collection runs.",
         "pm.collectionVariables.set('cbOccurredAt', '2026-08-01T11:00:00Z');",
         "pm.collectionVariables.set('cbEarlierAt', '2026-08-01T10:59:00Z');",
         "pm.collectionVariables.set('cbLaterAt', '2026-08-01T11:05:00Z');"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.test('PROCESSING', () => pm.expect(pm.response.json().status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Callback with no signature (401)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "description": "The HMAC is the authentication. There is no bearer token on this route and no other credential to fall back to."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: pm.collectionVariables.get('cbOccurredAt'), paymentIntentId: pm.collectionVariables.get('cbIntentId'), providerReference: pm.collectionVariables.get('cbReference'), outcome: 'SUCCEEDED', capturedAmountMinor: 2500 });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('says nothing about which check failed', () => pm.expect(pm.response.json().code).to.eql('PROVIDER_SIGNATURE_INVALID'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Callback signed over a different body (401)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "The body is inside the signed string, so a captured signature cannot be pasted onto a new body. Otherwise anyone who ever saw one callback could mark any payment succeeded."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: pm.collectionVariables.get('cbOccurredAt'), paymentIntentId: pm.collectionVariables.get('cbIntentId'), providerReference: pm.collectionVariables.get('cbReference'), outcome: 'SUCCEEDED', capturedAmountMinor: 2500 });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);",
         "// Sign one body, then send a different one.",
         "pm.collectionVariables.set('callbackBody', JSON.stringify(",
         "    Object.assign(JSON.parse(body), { capturedAmountMinor: 1 })",
         "));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));"
        ]
       }
      }
     ]
    },
    {
     "name": "Callback with a stale timestamp (401)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "The timestamp is signed, not merely sent, so it cannot be rewritten to refresh a captured signature. Anything more than 300 seconds out, in either direction, is refused."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: pm.collectionVariables.get('cbOccurredAt'), paymentIntentId: pm.collectionVariables.get('cbIntentId'), providerReference: pm.collectionVariables.get('cbReference'), outcome: 'SUCCEEDED', capturedAmountMinor: 2500 });",
         "// A perfectly valid signature, stamped 400 seconds ago.",
         "const t = Math.floor(Date.now() / 1000) - 400;",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));"
        ]
       }
      }
     ]
    },
    {
     "name": "REQUIRES_ACTION callback (200, APPLIED)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "The customer has a 3DS challenge to complete. The action URL is stored REDACTED -- origin and path survive, the query string does not, because it carries a challenge token."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: pm.collectionVariables.get('cbOccurredAt'), paymentIntentId: pm.collectionVariables.get('cbIntentId'), providerReference: pm.collectionVariables.get('cbReference'), outcome: 'REQUIRES_ACTION', actionUrl: 'https://3ds.simulator.test/challenge/abc?token=SECRET' });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);",
         "// Kept so the next two requests can re-deliver this exact event.",
         "pm.collectionVariables.set('cbChallengeEventId', JSON.parse(body).eventId);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('applied', () => pm.expect(pm.response.json().outcome).to.eql('APPLIED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Re-deliver the same event (200, DUPLICATE)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "A provider retries whether the response was lost or never sent. The row loses on pk_provider_callbacks, the transaction rolls back, and nothing happened -- no transition, no state-history row, no outbox event. 200 and never 409: a conflict here is an infinite retry loop."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: pm.collectionVariables.get('cbChallengeEventId'), occurredAt: pm.collectionVariables.get('cbOccurredAt'), paymentIntentId: pm.collectionVariables.get('cbIntentId'), providerReference: pm.collectionVariables.get('cbReference'), outcome: 'REQUIRES_ACTION', actionUrl: 'https://3ds.simulator.test/challenge/abc?token=SECRET' });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK, not 409', () => pm.response.to.have.status(200));",
         "pm.test('deduplicated', () => pm.expect(pm.response.json().outcome).to.eql('DUPLICATE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Re-confirm after the challenge (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{cbIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{cbIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbReconfirmKey}}"
       }
      ],
      "description": "REQUIRES_ACTION back to PROCESSING, opening a second attempt. THIS IS THE CYCLE the monotonic event clock exists for: inside it a stale REQUIRES_ACTION is a LEGAL transition, so the state machine alone cannot refuse it."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbReconfirmKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.test('PROCESSING again', () => pm.expect(pm.response.json().status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "A stale REQUIRES_ACTION arriving late (200, IGNORED_STALE)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "Its occurredAt is earlier than the event already applied, and the transition it asks for is legal. Only last_provider_event_at refuses it -- read as the maximum across the intent attempts, because the re-confirm above opened a new one whose own clock is null."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: pm.collectionVariables.get('cbEarlierAt'), paymentIntentId: pm.collectionVariables.get('cbIntentId'), providerReference: pm.collectionVariables.get('cbReference'), outcome: 'REQUIRES_ACTION', actionUrl: 'https://3ds.simulator.test/challenge/abc' });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('refused as stale', () => pm.expect(pm.response.json().outcome).to.eql('IGNORED_STALE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The payment succeeds (200, APPLIED)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "PROCESSING to SUCCEEDED. Operational state only: no balance moves and no ledger entry exists. The order behind it now reaches PAID once the relay delivers this event (ADR-016) -- see the \"Event delivery\" folder, which proves that end to end."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: pm.collectionVariables.get('cbLaterAt'), paymentIntentId: pm.collectionVariables.get('cbIntentId'), providerReference: pm.collectionVariables.get('cbReference'), outcome: 'SUCCEEDED', capturedAmountMinor: 2500 });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('applied', () => pm.expect(pm.response.json().outcome).to.eql('APPLIED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The intent is SUCCEEDED (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{cbIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{cbIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Read back through the merchant API. capturedAmountMinor is what the provider reported and what the intent authorized -- they have to agree."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('SUCCEEDED', () => pm.expect(b.status).to.eql('SUCCEEDED'));",
         "pm.test('captured what was owed', () => pm.expect(b.capturedAmountMinor).to.eql(2500));"
        ]
       }
      }
     ]
    },
    {
     "name": "A late callback for a finished payment (200, IGNORED_TERMINAL)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "Terminal states absorb. The second out-of-order mechanism, and not the clock: this event is NEWER than the one that succeeded, and it is still refused."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: '2026-08-01T11:10:00Z', paymentIntentId: pm.collectionVariables.get('cbIntentId'), providerReference: pm.collectionVariables.get('cbReference'), outcome: 'FAILED', failureCode: 'do_not_honour', failureMessage: 'Issuer declined' });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK, not 409', () => pm.response.to.have.status(200));",
         "pm.test('refused as terminal', () => pm.expect(pm.response.json().outcome).to.eql('IGNORED_TERMINAL'));"
        ]
       }
      }
     ]
    },
    {
     "name": "A callback naming an unknown intent (404)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "THE ONE NON-2XX, and it is a deliberate request to retry: the likeliest cause is a callback overtaking the transaction that created the intent. Nothing is stored -- there is nothing to deduplicate for an event that had no effect."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: pm.collectionVariables.get('cbOccurredAt'), paymentIntentId: 'pi_' + pm.variables.replaceIn('{{$guid}}'), providerReference: pm.collectionVariables.get('cbReference'), outcome: 'SUCCEEDED', capturedAmountMinor: 2500 });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('names the intent', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a second order (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbOrder2Key}}"
       }
      ],
      "description": "For the amount-mismatch and merchant-cancel demonstrations, which need an intent that has not finished."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbOrder2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('cbOrder2Id', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a second intent (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{cbOrder2Id}}\",\n  \"amountMinor\": 2500,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbIntent2Key}}"
       }
      ],
      "description": "Created here; attached and confirmed by the two requests after it."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbIntent2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('cbSecondIntentId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach to the second intent (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{cbSecondIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{cbSecondIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"UPI\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbAttach2Key}}"
       }
      ],
      "description": "REQUIRES_CONFIRMATION."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbAttach2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: confirm the second intent (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{cbSecondIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{cbSecondIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbConfirm2Key}}"
       }
      ],
      "description": "PROCESSING."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbConfirm2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));"
        ]
       }
      }
     ]
    },
    {
     "name": "A callback claiming the wrong amount (200, IGNORED_TERMINAL)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "A provider does not get to change what is owed (SDD 12.3). Run against a fresh intent, because the one above has already succeeded."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: '2026-08-01T11:00:00Z', paymentIntentId: pm.collectionVariables.get('cbSecondIntentId'), providerReference: 'sim_pay_' + pm.variables.replaceIn('{{$guid}}'), outcome: 'SUCCEEDED', capturedAmountMinor: 9999 });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('not applied', () => pm.expect(pm.response.json().outcome).to.eql('IGNORED_TERMINAL'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Park the second intent at REQUIRES_ACTION (200, APPLIED)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "The customer is sent to a 3DS challenge and abandons it."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: '2026-08-01T12:00:00Z', paymentIntentId: pm.collectionVariables.get('cbSecondIntentId'), providerReference: 'sim_pay_' + pm.variables.replaceIn('{{$guid}}'), outcome: 'REQUIRES_ACTION', actionUrl: 'https://3ds.simulator.test/challenge/xyz' });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('applied', () => pm.expect(pm.response.json().outcome).to.eql('APPLIED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel the abandoned challenge (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{cbSecondIntentId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{cbSecondIntentId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"customer abandoned the 3DS challenge\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{cbCancel2Key}}"
       }
      ],
      "description": "REQUIRES_ACTION to CANCELLED, and this transition is not optional (ADR-011). Without it an abandoned challenge holds the order's only slot forever and the order is dead in both directions."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('cbCancel2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('CANCELLED', () => pm.expect(pm.response.json().status).to.eql('CANCELLED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The customer completes it anyway (200, IGNORED_TERMINAL)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "PAYMESH AND THE PROVIDER NOW GENUINELY DISAGREE, and this row is the only trace of it. The callback is refused and recorded; resolving the divergence is reconciliation's job, and no reconciler exists yet. The race is real and ADR-012 says so rather than pretending otherwise."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED. A fixed signature would be valid for one",
         "// body at one second and would rot the moment either changed -- which is the property the",
         "// scheme exists to have, so hard-coding one would be testing nothing.",
         "const body = JSON.stringify({ eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'), occurredAt: '2026-08-01T12:05:00Z', paymentIntentId: pm.collectionVariables.get('cbSecondIntentId'), providerReference: 'sim_pay_' + pm.variables.replaceIn('{{$guid}}'), outcome: 'SUCCEEDED', capturedAmountMinor: 2500 });",
         "const t = Math.floor(Date.now() / 1000);",
         "// The signed string is t + '.' + body, so a captured signature cannot be replayed onto a",
         "// different body or a different time. HMAC-SHA256, hex, with the shared secret.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane instead would sign one string and",
         "// send another the first time a variable resolved differently.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK, not 409', () => pm.response.to.have.status(200));",
         "pm.test('refused as terminal', () => pm.expect(pm.response.json().outcome).to.eql('IGNORED_TERMINAL'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The intent is still CANCELLED (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{cbSecondIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{cbSecondIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Nothing the provider said moved it, and nothing captured."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('CANCELLED', () => pm.expect(b.status).to.eql('CANCELLED'));",
         "pm.test('captured nothing', () => pm.expect(b.capturedAmountMinor).to.eql(0));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Payment Intents - manual capture",
   "description": "AUTHORIZED -> SUCCEEDED at the merchant's request, and AUTHORIZED -> CANCELLED when they decide against it (design spec 5, ADR-011). Runs top to bottom and depends on the earlier folders for {{accessToken}}, {{otherAccessToken}} and {{providerCallbackSecret}}.\n\nReaching AUTHORIZED needs a provider callback, because only a provider can authorize -- so two requests here are HMAC-signed against the internal endpoint, with the signature computed in a pre-request script rather than pasted.\n\nThis folder completes the intent state machine: after it every status is reachable except PARTIALLY_REFUNDED and REFUNDED, which belong to the Refund capability.",
   "item": [
    {
     "name": "Setup: an order to authorize and capture (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capOrderKey}}"
       }
      ],
      "description": "Its own order. Every intent earlier in this collection has already taken and released a slot, and an order holds at most one live intent (ADR-011)."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capOrderKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('capOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a MANUAL-capture intent for it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{capOrderId}}\",\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"MANUAL\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capIntentKey}}"
       }
      ],
      "description": "captureMethod MANUAL is what makes this intent capturable at all. An AUTOMATIC intent is captured by the provider callback that reports SUCCEEDED, and a manual capture on one is refused 409 -- proved further down."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('MANUAL', () => pm.expect(b.captureMethod).to.eql('MANUAL'));",
         "pm.test('nothing captured yet', () => pm.expect(b.capturedAmountMinor).to.eql(0));",
         "pm.collectionVariables.set('capIntentId', b.id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach a payment method (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capAttachKey}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capAttachKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('REQUIRES_CONFIRMATION', () => pm.expect(pm.response.json().status).to.eql('REQUIRES_CONFIRMATION'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Capture before there is anything to capture (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "capture"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capEarlyKey}}"
       }
      ],
      "description": "Capture is legal from AUTHORIZED and nowhere else. 409 rather than 400: the request is well-formed and retrying it identically will never succeed."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capEarlyKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('the code names the state machine', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_CAPTURABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: confirm it (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"returnUrl\": \"https://shop.test/return\",\n  \"device\": \"web\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capConfirmKey}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capConfirmKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.test('PROCESSING', () => pm.expect(pm.response.json().status).to.eql('PROCESSING'));",
         "pm.collectionVariables.set('capReference', 'sim_pay_' + pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: the provider authorizes it (200, APPLIED)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "ONLY A PROVIDER CAN AUTHORIZE, so this precondition cannot be reached over the merchant-facing API at all. The signature is computed here, never pasted."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// AUTHORIZED, not SUCCEEDED: the provider is holding the funds and has not moved them.",
         "// captured_amount_minor stays 0 -- writing the authorized figure there would make an",
         "// uncaptured payment look collected to every reader of that column.",
         "const body = JSON.stringify({",
         "    eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'),",
         "    occurredAt: new Date().toISOString(),",
         "    paymentIntentId: pm.collectionVariables.get('capIntentId'),",
         "    providerReference: pm.collectionVariables.get('capReference'),",
         "    outcome: 'AUTHORIZED',",
         "    authorizedAmountMinor: 4000",
         "});",
         "const t = Math.floor(Date.now() / 1000);",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('applied', () => pm.expect(pm.response.json().outcome).to.eql('APPLIED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The intent is AUTHORIZED and has collected nothing (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ]
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('AUTHORIZED', () => pm.expect(b.status).to.eql('AUTHORIZED'));",
         "pm.test('an authorization has captured nothing', () => pm.expect(b.capturedAmountMinor).to.eql(0));"
        ]
       }
      }
     ]
    },
    {
     "name": "Capture more than was authorized (422)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "capture"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 4001\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capOverKey}}"
       }
      ],
      "description": "422, not 409: a capture IS possible on this intent and a smaller amount would succeed. ck_payment_intents_captured is the actual guarantee -- the aggregate's check exists so the answer is this and not a 500 naming a PostgreSQL constraint."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capOverKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Entity', () => pm.response.to.have.status(422));",
         "pm.test('the code names the amount, not the state', () => pm.expect(pm.response.json().code).to.eql('CAPTURE_AMOUNT_EXCEEDS_AUTHORIZED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Capture zero (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "capture"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 0\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capZeroKey}}"
       }
      ],
      "description": "Zero is malformed rather than merely impossible, so it is refused at the boundary. A merchant who wants to collect nothing cancels the authorization instead."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capZeroKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));"
        ]
       }
      }
     ]
    },
    {
     "name": "Partial capture (200, SUCCEEDED below the authorized amount)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "capture"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 2500\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capPartialKey}}"
       }
      ],
      "description": "200 and not 202, which is the opposite of confirm and deliberate: capture's outcome is decided by the time the response is written. The intent reaches SUCCEEDED with capturedAmountMinor BELOW amountMinor -- that gap is what will make orders.PARTIALLY_PAID reachable through the payment.succeeded consumer that does not exist yet. SUCCEEDED moves no balance and posts no ledger entry (spec 0.5)."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capPartialKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('SUCCEEDED', () => pm.expect(b.status).to.eql('SUCCEEDED'));",
         "pm.test('captured what was asked for', () => pm.expect(b.capturedAmountMinor).to.eql(2500));",
         "pm.test('and less than was authorized', () => pm.expect(b.capturedAmountMinor).to.be.below(b.amountMinor));",
         "pm.test('nothing refunded -- Refund does not exist', () => pm.expect(b.refundedAmountMinor).to.eql(0));"
        ]
       }
      }
     ]
    },
    {
     "name": "Capture again on the SAME key (200, replayed)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "capture"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 2500\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capPartialKey}}"
       }
      ],
      "description": "A retried capture must REPLAY, not conflict. This is the route on the idempotent list that most needs to be there: a network retry of a collection that already committed should get its own answer back, not a 409."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('replayed, not re-executed', () => pm.expect(pm.response.headers.get('Idempotency-Replayed')).to.eql('true'));",
         "pm.test('same captured amount', () => pm.expect(pm.response.json().capturedAmountMinor).to.eql(2500));"
        ]
       }
      }
     ]
    },
    {
     "name": "Capture again on a FRESH key (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "capture"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 1500\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capSecondKey}}"
       }
      ],
      "description": "A genuinely new request to collect the rest is refused. Partial capture is one shot in this design -- the intent is SUCCEEDED and no longer capturable -- and the uncollected 1500 stays uncollected."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capSecondKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('not capturable', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_CAPTURABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel a captured intent (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"second thoughts\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capCancelKey}}"
       }
      ],
      "description": "Captured funds cannot be un-captured by cancelling. Reversal is the Refund capability's, and it does not exist."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capCancelKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('not cancellable', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_CANCELLABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Capture hands the order to the relay (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{capOrderId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{capOrderId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "THIS USED TO ASSERT \"still PENDING\", AND THAT CLAIM IS WHAT ADR-016 DELETED. Payment still does not write the orders table (design spec 0.5) -- but Order now consumes payment.succeeded and moves its own status, so a partial capture ends as PARTIALLY_PAID once the relay delivers.\n\nDeliberately accepts EITHER value. This request runs milliseconds after the capture, so which one it sees depends on whether a 2s relay tick has fired -- and with the relay off (the dev profile default) it stays PENDING forever. Asserting either specific value here would be a test that passes on timing. The deterministic proof lives in the \"Event delivery\" folder, which polls."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "// 2500, not the 3000 this line used to say. The capture above is 2500 and always was;",
         "// the wrong figure survived because this assertion only ever ran in the PENDING branch,",
         "// where amountPaidMinor is 0 and the other side of the ternary is never evaluated. A",
         "// relay tick landing before this read is all it took to expose it.",
         "const b = pm.response.json();",
         "// PARTIALLY_PAID because the capture above took 2500 of an authorized 4000. PENDING if the",
         "// relay has not ticked yet, or is switched off. Both are correct; neither is assertable alone.",
         "pm.test('PENDING or PARTIALLY_PAID, depending on the relay', () =>",
         "    pm.expect(b.status).to.be.oneOf(['PENDING', 'PARTIALLY_PAID']));",
         "pm.test('amountPaidMinor agrees with the status', () =>",
         "    pm.expect(b.amountPaidMinor).to.eql(b.status === 'PENDING' ? 0 : 2500));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a second order, to release rather than collect (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capOrder2Key}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capOrder2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('capOrder2Id', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a MANUAL intent, attached and confirmed (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{capOrder2Id}}\",\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"MANUAL\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capIntent2Key}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capIntent2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('capIntent2Id', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach to it (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntent2Id}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntent2Id}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"UPI\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capAttach2Key}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capAttach2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: confirm it (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntent2Id}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntent2Id}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capConfirm2Key}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capConfirm2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.collectionVariables.set('capReference2', 'sim_pay_' + pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: the provider authorizes it too (200, APPLIED)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ]
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const body = JSON.stringify({",
         "    eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'),",
         "    occurredAt: new Date().toISOString(),",
         "    paymentIntentId: pm.collectionVariables.get('capIntent2Id'),",
         "    providerReference: pm.collectionVariables.get('capReference2'),",
         "    outcome: 'AUTHORIZED',",
         "    authorizedAmountMinor: 4000",
         "});",
         "const t = Math.floor(Date.now() / 1000);",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('applied', () => pm.expect(pm.response.json().outcome).to.eql('APPLIED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cancel the authorization instead of capturing it (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntent2Id}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntent2Id}}",
        "cancel"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"reason\": \"out of stock after all\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capCancel2Key}}"
       }
      ],
      "description": "AUTHORIZED -> CANCELLED, which ADR-011's slot table REQUIRES. A MANUAL intent parked at AUTHORIZED is a state a merchant can sit in indefinitely; without this route the order's only live slot is held forever by funds nobody intends to take, and a slot that cannot be released kills the order."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capCancel2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('CANCELLED', () => pm.expect(b.status).to.eql('CANCELLED'));",
         "pm.test('and nothing was collected', () => pm.expect(b.capturedAmountMinor).to.eql(0));",
         "pm.test('the cancellation is stamped', () => pm.expect(b.cancelledAt).to.not.be.null);"
        ]
       }
      }
     ]
    },
    {
     "name": "The order's slot is free again (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{capOrder2Id}}\",\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capIntent3Key}}"
       }
      ],
      "description": "THE CANCEL ACTUALLY RELEASED THE SLOT, proved rather than assumed: uq_payment_intents_live_per_order excludes exactly FAILED and CANCELLED, so this create would be refused 409 if the previous request had not really freed it."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capIntent3Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.test('a fresh collection may be started', () => pm.expect(pm.response.json().status).to.eql('REQUIRES_PAYMENT_METHOD'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Cross-tenant: capture another merchant's intent (404, not 403)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "capture"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{capCrossKey}}"
       }
      ],
      "description": "A pi_ in a path authorizes nothing. 404 rather than 403, because a 403 would confirm the id exists and turn this endpoint into an oracle for enumerating another tenant's payments (ADR-007)."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('capCrossKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('as if it never existed', () => pm.expect(pm.response.json().code).to.eql('PAYMENT_INTENT_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Capture with no Idempotency-Key (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{capIntentId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{capIntentId}}",
        "capture"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Capture is a registered idempotent route, so the header is mandatory. It is the route on that list that takes funds."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('the key is required', () => pm.expect(pm.response.json().code).to.eql('IDEMPOTENCY_KEY_REQUIRED'));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Event delivery (outbox relay to order.PAID)",
   "description": "THE LOOP THAT DID NOT CLOSE UNTIL ADR-016: create an order, collect against it, and watch the ORDER reach PAID without anyone calling an order endpoint.\n\nPayment writes payment.succeeded to outbox_events in the same transaction as the payment (ADR-010). The relay reads it, the dispatcher hands it to Order's consumer, and Order moves its own status and amountPaidMinor. Payment still never writes the orders table, which is why ModuleBoundaryTest.orderNeverImportsPayment keeps an empty allowlist.\n\nSTART THE APP WITH THE RELAY ON:\n    PAYMESH_EVENTS_OUTBOX_RELAY_ENABLED=true ./mvnw spring-boot:run\nThe dev profile that spring-boot:run activates switches the relay OFF, because that same profile is what the test suite runs under and a timer moving orders to PAID mid-assertion is a flake generator. Without the variable, the last request here polls ten times and fails.\n\nRuns after the folders above and reuses their access token; it mints its own order, intent and idempotency keys, so it disturbs nothing they left behind.",
   "item": [
    {
     "name": "Setup: an order for the relay to settle (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{edOrderKey}}"
       }
      ],
      "description": "Its own order. An order holds at most one live payment intent (ADR-011), so every folder above has already spent the ones it created."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('edOrderKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('edIntentKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('edAttachKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('edConfirmKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('edReference', 'sim_pay_' + pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('providerCallbackSecret', 'dev-only-insecure-provider-callback-secret-change-me');",
         "// The poll below counts its own attempts. Reset here so a second run of the folder",
         "// does not start already exhausted.",
         "pm.collectionVariables.set('edPollAttempt', 0);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.test('PENDING, with nothing paid', () => {",
         "    const b = pm.response.json();",
         "    pm.expect(b.status).to.eql('PENDING');",
         "    pm.expect(b.amountPaidMinor).to.eql(0);",
         "});",
         "pm.collectionVariables.set('edOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a payment intent for it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{edOrderId}}\",\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{edIntentKey}}"
       }
      ],
      "description": "AUTOMATIC capture, so the provider callback that reports SUCCEEDED is what collects -- no merchant capture step in this folder."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('edIntentId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach a payment method (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{edIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{edIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{edAttachKey}}"
       }
      ],
      "description": "Records the KIND of instrument, never the instrument. Moves no money and calls nothing."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: confirm it (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{edIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{edIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{edConfirmKey}}"
       }
      ],
      "description": "PROCESSING. 202 because the outcome is undecided until a callback resolves it."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// A fixed provider instant, so the payload signed below is stable within this run.",
         "pm.collectionVariables.set('edOccurredAt', '2026-08-02T11:00:00Z');"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.test('PROCESSING', () => pm.expect(pm.response.json().status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The provider collects the full amount (200, APPLIED)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/provider-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "provider-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{callbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{callbackSignature}}"
       }
      ],
      "description": "PROCESSING to SUCCEEDED, and the payment.succeeded row lands in outbox_events in the same transaction (ADR-010). Everything after this is delivery."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// THE SIGNATURE IS COMPUTED HERE, NOT PASTED -- same scheme as the provider-callbacks",
         "// folder above. A fixed signature would be valid for one body at one second.",
         "const body = JSON.stringify({",
         "    eventId: 'sim_evt_' + pm.variables.replaceIn('{{$guid}}'),",
         "    occurredAt: pm.collectionVariables.get('edOccurredAt'),",
         "    paymentIntentId: pm.collectionVariables.get('edIntentId'),",
         "    providerReference: pm.collectionVariables.get('edReference'),",
         "    outcome: 'SUCCEEDED',",
         "    capturedAmountMinor: 4000",
         "});",
         "const t = Math.floor(Date.now() / 1000);",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('providerCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{callbackBody}}, so what is sent is byte-for-byte",
         "// what was signed.",
         "pm.collectionVariables.set('callbackBody', body);",
         "pm.collectionVariables.set('callbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('applied', () => pm.expect(pm.response.json().outcome).to.eql('APPLIED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The payment is SUCCEEDED (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{edIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{edIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The money side is done. SUCCEEDED is operational state: no balance moved and no ledger entry exists (design spec 0.5). The obligation side has not moved yet."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('SUCCEEDED', () => pm.expect(b.status).to.eql('SUCCEEDED'));",
         "pm.test('for the whole amount', () => pm.expect(b.capturedAmountMinor).to.eql(4000));"
        ]
       }
      }
     ]
    },
    {
     "name": "The order reaches PAID once the relay delivers it (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{edOrderId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{edOrderId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "THE POINT OF THIS FOLDER, AND THE LINE THAT USED TO READ \"still PENDING\". Nobody called an order endpoint: the relay read payment.succeeded out of outbox_events, Order's consumer applied it, and Order moved its own column (ADR-016). Payment still never writes the orders table.\n\nIT POLLS, because delivery is asynchronous by design -- the relay runs on a 2s fixedDelay timer. Up to ten attempts, roughly a second and a half apart.\n\nREQUIRES THE RELAY TO BE ON. `./mvnw spring-boot:run` activates the dev profile, where it is switched off so it cannot mutate rows underneath the test suite. Start the app with PAYMESH_EVENTS_OUTBOX_RELAY_ENABLED=true or this request exhausts its attempts and fails, which is the honest outcome rather than a skip."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// A pre-request setTimeout is how a Postman run waits; the sandbox holds the request",
         "// until pending timers settle. The relay's default interval is 2s, so this is a little",
         "// under one tick per attempt and ten attempts covers a comfortable margin.",
         "setTimeout(() => {}, 1500);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const attempt = Number(pm.collectionVariables.get('edPollAttempt') || 0) + 1;",
         "pm.collectionVariables.set('edPollAttempt', attempt);",
         "",
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "",
         "if (b.status === 'PENDING' && attempt < 10) {",
         "    // Not delivered yet. Re-run THIS request rather than asserting -- a failure on the",
         "    // first poll would only be measuring how fast the timer happened to fire.",
         "    console.log('waiting for the relay, attempt ' + attempt + ' of 10');",
         "    postman.setNextRequest(pm.info.requestName);",
         "} else {",
         "    pm.test('PAID -- the relay delivered payment.succeeded and Order consumed it', () =>",
         "        pm.expect(b.status).to.eql('PAID'));",
         "    pm.test('and amountPaidMinor moved with it', () =>",
         "        pm.expect(b.amountPaidMinor).to.eql(4000));",
         "}"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Provider Simulator (/sim/v1, ADR-017)",
   "description": "THE PROVIDER, NOT THE MERCHANT API. Everything here is under /sim/v1 and none of it is part of the surface a merchant integrates against.\n\nAuthentication is X-PayMesh-Simulator-Key and nothing else -- not a bearer token, which is refused on purpose: these routes queue the callback that marks a payment SUCCEEDED, so a merchant able to call them could authorize their own collection (ADR-017 section 3).\n\nThe first eighteen requests need no background job and run against a plain `./mvnw spring-boot:run`. The last six drive a real payment intent end to end and need the callback dispatcher, which the dev profile switches OFF so it cannot mutate rows under a test. Start the server with PAYMESH_SIMULATOR_DISPATCH_ENABLED=true for those.\n\nDepends on Identity & Auth having captured {{accessToken}}, like every other folder here.",
   "item": [
    {
     "name": "Refuse a create carrying no simulator key",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "description": "The only authentication on /sim/v1/** is X-PayMesh-Simulator-Key. This route queues a callback that marks a payment SUCCEEDED, so an unauthenticated simulator would be an unauthenticated way to collect any payment on the platform (ADR-017 section 3).",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simKeyA}}\",\n  \"callbackReference\": \"pi_unauthenticated\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simKeyA', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('one code for every failure', () => pm.expect(pm.response.json().code).to.eql('SIMULATOR_KEY_INVALID'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refuse a merchant access token as a way in",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "THE ONE THAT MATTERS. A merchant able to drive the provider could authorize their own collection. The chain permits the route -- there is no bearer token to evaluate -- and the filter refuses it anyway, because a token is not one of the ways to produce the key.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simKeyB}}\",\n  \"callbackReference\": \"pi_merchant_token\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simKeyB', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('SIMULATOR_KEY_INVALID', () => pm.expect(pm.response.json().code).to.eql('SIMULATOR_KEY_INVALID'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refuse the wrong simulator key",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "not-the-key"
       }
      ],
      "description": "Constant-time comparison, and one answer with no detail about which check failed.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simKeyC}}\",\n  \"callbackReference\": \"pi_wrong_key\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simKeyC', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));"
        ]
       }
      }
     ]
    },
    {
     "name": "Take a payment the provider captures automatically",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "tok_sim_success with AUTOMATIC capture: the provider authorizes and captures in one step and queues a single SUCCEEDED callback. The behaviour is resolved once, at create time, and stored on the row -- changing the failure profile later cannot make this payment change its mind.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simSuccessKey}}\",\n  \"callbackReference\": \"pi_sim_walkthrough\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simSuccessKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('provider mints its own opaque id', () => pm.expect(b.providerPaymentId).to.match(/^sim_pay_[0-9a-f-]{36}$/));",
         "pm.test('CAPTURED', () => pm.expect(b.status).to.eql('CAPTURED'));",
         "pm.test('behaviour resolved from the token', () => pm.expect(b.behaviour).to.eql('SUCCEED'));",
         "pm.test('captured the full amount', () => pm.expect(b.capturedAmountMinor).to.eql(1999));",
         "pm.test('no PayMesh concepts leak into the provider resource', () => {",
         "  ['merchantId', 'orderId', 'paymentIntentId'].forEach(f => pm.expect(b).to.not.have.property(f));",
         "});",
         "pm.collectionVariables.set('simPaymentId', b.providerPaymentId);"
        ]
       }
      }
     ]
    },
    {
     "name": "Replay the identical request: 200, not a second payment",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "Provider-side idempotency, and deliberately NOT the platform's IdempotencyFilter -- that one keys on merchant + endpoint + key, and a provider has no merchant. uq_provider_payments_idempotency_key is the guard. 200 rather than 201 so a caller can tell a replay from a create without diffing bodies.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simSuccessKey}}\",\n  \"callbackReference\": \"pi_sim_walkthrough\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK, not 201', () => pm.response.to.have.status(200));",
         "pm.test('the same payment came back', () => pm.expect(pm.response.json().providerPaymentId).to.eql(pm.collectionVariables.get('simPaymentId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Same key, different amount: 409",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "Fails closed rather than returning the original. Some real providers return the original and it would be friendlier -- but the original may be for a different amount, and answering 'your 1999 payment succeeded' to a request for 50000 is a money-path lie.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simSuccessKey}}\",\n  \"callbackReference\": \"pi_sim_walkthrough\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 50000,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('SIMULATOR_IDEMPOTENCY_KEY_REUSED', () => pm.expect(pm.response.json().code).to.eql('SIMULATOR_IDEMPOTENCY_KEY_REUSED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "A declined payment",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "tok_sim_decline. One FAILED callback carrying the vocabulary a real card network uses.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simDeclineKey}}\",\n  \"callbackReference\": \"pi_sim_declined\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_decline\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simDeclineKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('DECLINED', () => pm.expect(b.status).to.eql('DECLINED'));",
         "pm.test('do_not_honour', () => pm.expect(b.failureCode).to.eql('do_not_honour'));",
         "pm.test('captured nothing', () => pm.expect(b.capturedAmountMinor).to.eql(0));"
        ]
       }
      }
     ]
    },
    {
     "name": "The lost callback: TIMED_OUT, and nothing is ever sent",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "tok_sim_timeout is the only behaviour that queues NO callback row at all. The payment is real at the provider and invisible to PayMesh -- which is exactly the stranded PROCESSING state ADR-015's timeout sweeper exists for, and until this module existed it could only be reached by hand.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simTimeoutKey}}\",\n  \"callbackReference\": \"pi_sim_timeout\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_timeout\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simTimeoutKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.test('TIMED_OUT', () => pm.expect(pm.response.json().status).to.eql('TIMED_OUT'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Take a payment the provider holds for manual capture",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "MANUAL stops at AUTHORIZED and queues an AUTHORIZED callback. ADR-012 section 4 refuses AUTHORIZED -> SUCCEEDED as a provider transition, so a simulator that captured on its own say-so would produce IGNORED_TERMINAL and look broken.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simManualKey}}\",\n  \"callbackReference\": \"pi_sim_manual\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"MANUAL\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simManualKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('AUTHORIZED', () => pm.expect(b.status).to.eql('AUTHORIZED'));",
         "pm.test('captured nothing yet', () => pm.expect(b.capturedAmountMinor).to.eql(0));",
         "pm.collectionVariables.set('simManualId', b.providerPaymentId);"
        ]
       }
      }
     ]
    },
    {
     "name": "Capture the authorization",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments/{{simManualId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments",
        "{{simManualId}}",
        "capture"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "Settles the authorization and queues the SUCCEEDED callback.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simCaptureKey}}\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simCaptureKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('CAPTURED', () => pm.expect(b.status).to.eql('CAPTURED'));",
         "pm.test('captured the authorized amount', () => pm.expect(b.capturedAmountMinor).to.eql(1999));"
        ]
       }
      }
     ]
    },
    {
     "name": "Capturing twice is refused",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments/{{simManualId}}/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments",
        "{{simManualId}}",
        "capture"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "The one that would collect twice. Refused by the aggregate rather than left to the CHECK constraint, so the caller gets a 409 naming the state instead of a 500 naming an index.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simCapture2Key}}\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simCapture2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('SIMULATED_PAYMENT_NOT_CAPTURABLE', () => pm.expect(pm.response.json().code).to.eql('SIMULATED_PAYMENT_NOT_CAPTURABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Capturing an unknown payment is 404",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments/sim_pay_00000000-0000-4000-8000-000000000000/capture",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments",
        "sim_pay_00000000-0000-4000-8000-000000000000",
        "capture"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "404 rather than the merchant API's deliberate ambiguity: there is one credential and no tenants here, so a caller holding the key is entitled to know an id is unknown.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simCapture3Key}}\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simCapture3Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('SIMULATED_PAYMENT_NOT_FOUND', () => pm.expect(pm.response.json().code).to.eql('SIMULATED_PAYMENT_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refund half of what was captured",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/refunds",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "refunds"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "The refund row is the provider's truth and appears in the reconciliation export. NO CALLBACK IS QUEUED, deliberately: /internal/v1/provider-callbacks speaks only the four payment outcomes, so a refund callback today would retry into a 404 until ABANDONED.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simRefundKey}}\",\n  \"providerPaymentId\": \"{{simPaymentId}}\",\n  \"amountMinor\": 999\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simRefundKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('sim_ref_ prefix, not ref_', () => pm.expect(b.providerRefundId).to.match(/^sim_ref_[0-9a-f-]{36}$/));",
         "pm.test('SUCCEEDED', () => pm.expect(b.status).to.eql('SUCCEEDED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refunding more than remains is refused",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/refunds",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "refunds"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "1999 captured, 999 already refunded, so 1000 remains. The application checks it under a row lock for a readable 422; ck_provider_payments_refunded is what actually guarantees it.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simRefund2Key}}\",\n  \"providerPaymentId\": \"{{simPaymentId}}\",\n  \"amountMinor\": 1500\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simRefund2Key', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Entity', () => pm.response.to.have.status(422));",
         "pm.test('REFUND_EXCEEDS_CAPTURED_AMOUNT', () => pm.expect(pm.response.json().code).to.eql('REFUND_EXCEEDS_CAPTURED_AMOUNT'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Configure the ambient failure profile",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/failure-profile",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "failure-profile"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "Last-write-wins configuration, so 200 and not 201, and deliberately not idempotency-keyed: a replayed configuration change is the same configuration. The token still wins wherever it names a behaviour -- this only fills in where it names nothing.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"defaultBehaviour\": \"SUCCEED\",\n  \"callbackDelayMs\": 0\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('SUCCEED', () => pm.expect(pm.response.json().defaultBehaviour).to.eql('SUCCEED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "An unknown behaviour is refused without naming a Java type",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/failure-profile",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "failure-profile"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "An error body is not a place to hand a caller internal type names.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"defaultBehaviour\": \"EXPLODE\",\n  \"callbackDelayMs\": 0\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('INVALID_REQUEST', () => pm.expect(pm.response.json().code).to.eql('INVALID_REQUEST'));",
         "pm.test('no Java type in the message', () => pm.expect(pm.response.json().message).to.not.include('SimulatedBehaviour.'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Export the provider's own truth for today",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/reconciliation/{{simToday}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "reconciliation",
        "{{simToday}}"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "ADR-015 leans on a reconciliation job as the mitigation for timing a stranded PROCESSING payment out to FAILED. THIS IS THE INPUT, NOT THE JOB -- nothing here compares the provider's truth to PayMesh's, and no payment changes state because a report was read. TIMED_OUT in countsByStatus is the interesting bucket: payments the provider decided and never reported."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simToday', new Date().toISOString().slice(0, 10));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('the day it was asked for', () => pm.expect(b.date).to.eql(pm.collectionVariables.get('simToday')));",
         "pm.test('carries this run\\'s payment', () => pm.expect(b.payments.map(p => p.providerPaymentId)).to.include(pm.collectionVariables.get('simPaymentId')));",
         "pm.test('counts are per provider-side status', () => pm.expect(b.countsByStatus).to.be.an('object'));"
        ]
       }
      }
     ]
    },
    {
     "name": "A day the provider took nothing is an empty report, not a 404",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/reconciliation/2020-01-01",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "reconciliation",
        "2020-01-01"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "A date with no activity is an honest empty answer, not a missing resource."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('no payments', () => pm.expect(pm.response.json().payments).to.be.empty);",
         "pm.test('nothing captured', () => pm.expect(pm.response.json().capturedTotalMinor).to.eql(0));"
        ]
       }
      }
     ]
    },
    {
     "name": "End to end: an order for the simulator to collect",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{simE2eOrderKey}}"
       }
      ],
      "description": "REQUIRES THE DISPATCHER. Everything above works with the timer off; the four requests from here on need it, because they wait for a callback to actually be delivered. ./mvnw spring-boot:run activates the dev profile, which sets paymesh.simulator.dispatch.enabled=false -- start the server with PAYMESH_SIMULATOR_DISPATCH_ENABLED=true or these will time out.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"merchantOrderReference\": \"SIM-E2E-{{simE2eRef}}\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"description\": \"Collected by the provider simulator\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simE2eRef', pm.variables.replaceIn('{{$randomInt}}'));",
         "pm.collectionVariables.set('simE2eOrderKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('simE2eOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "End to end: create and confirm the intent (PROCESSING)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{simE2eIntentKey}}"
       }
      ],
      "description": "Created here; attached and confirmed by the next two requests.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{simE2eOrderId}}\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simE2eIntentKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('simE2eIntentId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "End to end: attach a card",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{simE2eIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{simE2eIntentId}}",
        "payment-method"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{simE2eAttachKey}}"
       }
      ],
      "description": "Attach, then confirm. The intent cannot be confirmed without an instrument.\n\nThe Idempotency-Key is REQUIRED: attach is a registered idempotent route, and without it this answers 400 IDEMPOTENCY_KEY_REQUIRED and every request after it in this chain fails on the state it never reached.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simE2eAttachKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));"
        ]
       }
      }
     ]
    },
    {
     "name": "End to end: confirm, leaving the intent PROCESSING",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{simE2eIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{simE2eIntentId}}",
        "confirm"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{simE2eConfirmKey}}"
       }
      ],
      "description": "PROCESSING is the state with no local exit -- cancel is refused from it, because an in-flight attempt may already have succeeded at the provider. Only a callback moves it, and the next request is what makes one happen.",
      "body": {
       "mode": "raw",
       "raw": "{}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simE2eConfirmKey', pm.variables.replaceIn('{{$guid}}'));"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.test('PROCESSING', () => pm.expect(pm.response.json().status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "End to end: tell the provider to collect it",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "callbackReference is the intent id, and the simulator neither knows nor cares what it is -- it is stored, echoed into the callback body, and never interpreted. A real provider models exactly this as a merchant reference passthrough.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{simE2ePayKey}}\",\n  \"callbackReference\": \"{{simE2eIntentId}}\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 1999,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simE2ePayKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('simE2ePolls', 0);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.test('CAPTURED at the provider', () => pm.expect(pm.response.json().status).to.eql('CAPTURED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "End to end: poll until the callback lands and the intent is SUCCEEDED",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{simE2eIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{simE2eIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "THE ASSERTION THE WHOLE FOLDER EXISTS FOR, and the first one in this collection that is not immediate. The dispatcher runs every two seconds, so this re-requests itself until the intent leaves PROCESSING -- up to about thirty seconds. A timeout here almost always means the server was started without PAYMESH_SIMULATOR_DISPATCH_ENABLED=true."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const status = pm.response.json().status;",
         "const polls = Number(pm.collectionVariables.get('simE2ePolls') || 0);",
         "",
         "if (status === 'PROCESSING' && polls < 15) {",
         "  pm.collectionVariables.set('simE2ePolls', polls + 1);",
         "  setTimeout(() => {}, 2000);",
         "  postman.setNextRequest('End to end: poll until the callback lands and the intent is SUCCEEDED');",
         "} else {",
         "  pm.test('the simulator drove the intent to SUCCEEDED', () => pm.expect(status).to.eql('SUCCEEDED'));",
         "  pm.test('for exactly the order amount', () => pm.expect(pm.response.json().capturedAmountMinor).to.eql(1999));",
         "  // NO setNextRequest(null) HERE. It used to say that, and it halted the WHOLE RUN --",
         "  // harmless while this was the last folder in the collection, and silently fatal the",
         "  // moment anything was added after it. The Ledger and Refund folders were unreachable",
         "  // in a full newman run until this line was deleted. Falling off the end of a script",
         "  // continues to the next request, which is what was wanted all along.",
         "}"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Ledger (a payment becomes a balance, ADR-018)",
   "description": "WHERE THE MONEY IS ACTUALLY WRITTEN DOWN. Before ADR-018 a payment reaching SUCCEEDED meant PayMesh held a row saying a payment succeeded -- it did not mean anybody's money had moved, because there was nowhere for money to be.\n\nThis folder collects 99900 and watches the merchant's pending balance rise by exactly that. Nothing here calls a ledger endpoint, because there is no way to write to the ledger over HTTP: the only writer is a consumer of payment.succeeded, which means every posting traces back to a committed payment. SDD 15.3's POST /internal/v1/ledger/transactions is deliberately absent (ADR-018 section 3).\n\nDouble-entry, so the capture is written twice and the two sides must match:\n    DEBIT   provider-clearing:INR        99900   <- the provider owes us\n    CREDIT  merchant:<you>:pending:INR   99900   <- we owe the merchant\nThat rule is enforced by a DEFERRED PostgreSQL trigger checked at COMMIT, not by Java, and so is the immutability of a posted entry. A correction is a new reversal journal, never an edit -- which is what Refund will have to write.\n\nSTART THE APP WITH BOTH TIMERS ON, or the balance never moves:\n    PAYMESH_SIMULATOR_DISPATCH_ENABLED=true \\\n    PAYMESH_EVENTS_OUTBOX_RELAY_ENABLED=true \\\n    ./mvnw spring-boot:run\n\nRuns after the folders above and reuses their access token; it mints its own order, intent and idempotency keys, so it disturbs nothing they left behind.",
   "item": [
    {
     "name": "Baseline: the balance before anything is collected (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/balances",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "balances"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The starting point, and it is deliberately read BEFORE the payment so the delta at the end means something.\n\nThis merchant has been paid by earlier folders, so the figure is usually not zero -- what matters is that it moves by exactly the amount collected below. A merchant who has never been paid gets an empty list here, not a 404: 'you have earned nothing yet' is not an error."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('lgOrderKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('lgIntentKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('lgAttachKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('lgConfirmKey', pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('lgReference', 'sim_pay_' + pm.variables.replaceIn('{{$guid}}'));",
         "pm.collectionVariables.set('lgPollAttempt', 0);",
         "// A pause each pass so the relay has a tick to drain between the two reads the settle",
         "// check compares.",
         "setTimeout(() => {}, 1500);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// SETTLE BEFORE MEASURING. Every folder shares one merchant, and the relay moves that",
         "// merchant's balance asynchronously -- so an earlier folder's collection can land in the",
         "// middle of this one and be counted as ours. Poll until two consecutive reads agree, which",
         "// means the backlog is drained, and only then record the baseline.",
         "//",
         "// Without this the delta assertions below measure whatever happened to be in flight. That is",
         "// exactly how they failed the first time this collection was run end to end.",
         "const inr = pm.response.json().balances.find(r => r.currency === 'INR');",
         "const now = inr ? inr.pendingMinor : 0;",
         "const last = pm.collectionVariables.get('lgSettleLast');",
         "const tries = Number(pm.collectionVariables.get('lgSettleTries') || 0) + 1;",
         "",
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "",
         "if ((last === undefined || last === '' || Number(last) !== now) && tries < 20) {",
         "    pm.collectionVariables.set('lgSettleLast', now);",
         "    pm.collectionVariables.set('lgSettleTries', tries);",
         "    postman.setNextRequest(pm.info.requestName);",
         "} else {",
         "    pm.collectionVariables.set('lgBalanceBefore', now);",
         "    console.log('settled baseline: ' + now);",
         "}"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: an order for the ledger to record (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 99900,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{lgOrderKey}}"
       }
      ],
      "description": "Its own order -- an order holds at most one live payment intent (ADR-011), so the folders above have already spent theirs."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('lgOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a payment intent against it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{lgOrderId}}\",\n  \"amountMinor\": 99900,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{lgIntentKey}}"
       }
      ],
      "description": "AUTOMATIC capture, so the provider's SUCCEEDED callback collects without a second call."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('lgIntentId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach a card (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{lgIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{lgIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{lgAttachKey}}"
       }
      ],
      "description": "An intent must carry a payment method before it can be confirmed.\n\nThe Idempotency-Key is REQUIRED here -- attach is a registered idempotent route, and without the header this answers 400 IDEMPOTENCY_KEY_REQUIRED rather than attaching anything. Found by running this folder against a live app rather than by reading the controller."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: confirm, sending it to the provider (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{lgIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{lgIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{lgConfirmKey}}"
       }
      ],
      "description": "PROCESSING. From here only a provider callback can move it, which is what the next request is."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.test('PROCESSING', () => pm.expect(pm.response.json().status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The simulator collects it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{lgReference}}\",\n  \"callbackReference\": \"{{lgIntentId}}\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 99900,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "The fake provider takes the charge and queues a signed callback (ADR-017). Nothing in PayMesh knows it is fake -- the callback arrives over HTTP at /internal/v1/provider-callbacks exactly as a real one would.\n\ntok_sim_success, so the callback reports SUCCEEDED for the full amount."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.test('CAPTURED at the provider', () =>",
         "    pm.expect(pm.response.json().status).to.eql('CAPTURED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "THE POINT: the balance moves by exactly what was collected (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/balances",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "balances"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "THE LINE THIS WHOLE FOLDER EXISTS FOR. Nobody called a ledger endpoint -- there isn't one to call. Payment wrote payment.succeeded, the relay read it, and the Ledger's own consumer posted a balanced journal (ADR-018):\n\n    DEBIT   provider-clearing:INR        99900\n    CREDIT  merchant:<you>:pending:INR   99900\n\nThe figure below is not a stored column. It is a SUM over the immutable entries, computed when you ask -- so it cannot drift from the journal the way a cached projection could.\n\nIT POLLS, because delivery is asynchronous by design. Two things must both have happened: the simulator's dispatcher must have sent the callback, and the outbox relay must have delivered the event.\n\nREQUIRES BOTH TIMERS ON:\n    PAYMESH_SIMULATOR_DISPATCH_ENABLED=true \\\n    PAYMESH_EVENTS_OUTBOX_RELAY_ENABLED=true \\\n    ./mvnw spring-boot:run\nThe dev profile switches both off, because it is the profile the test suite runs under and a timer moving money mid-assertion is a flake generator. Without them this request exhausts its attempts and fails, which is the honest outcome rather than a skip."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// A pre-request setTimeout is how a Postman run waits; the sandbox holds the request until",
         "// pending timers settle. Two hops here rather than one, so the wait is longer than the",
         "// event-delivery folder's.",
         "setTimeout(() => {}, 2000);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const attempt = Number(pm.collectionVariables.get('lgPollAttempt') || 0) + 1;",
         "pm.collectionVariables.set('lgPollAttempt', attempt);",
         "",
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const rows = pm.response.json().balances;",
         "const inr = rows.find(r => r.currency === 'INR');",
         "const now = inr ? inr.pendingMinor : 0;",
         "const before = Number(pm.collectionVariables.get('lgBalanceBefore'));",
         "",
         "if (now === before && attempt < 15) {",
         "    console.log('waiting for the callback and the relay, attempt ' + attempt + ' of 15');",
         "    postman.setNextRequest(pm.info.requestName);",
         "} else {",
         "    pm.test('pending balance rose by exactly the captured amount', () =>",
         "        pm.expect(now - before).to.eql(99900));",
         "}"
        ]
       }
      }
     ]
    },
    {
     "name": "Reading it again changes nothing (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/balances",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "balances"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "A balance read is a read. Worth one request because the ledger's idempotency is easy to assume and hard to see: the same payment cannot post twice however many times its event is delivered.\n\nTwo independent guards make that true. processed_events (V14) stops one EVENT being applied twice by one consumer. uq_ledger_transactions_idempotency, keyed on 'payment-captured:<intent id>', stops one PAYMENT being posted twice however many events describe it -- a replayed backlog, a renamed consumer, a manual re-run.\n\nLeave the app running and re-run this folder's last two requests as often as you like; the figure does not move again."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const inr = pm.response.json().balances.find(r => r.currency === 'INR');",
         "const before = Number(pm.collectionVariables.get('lgBalanceBefore'));",
         "pm.test('still exactly one posting for that payment', () =>",
         "    pm.expect(inr.pendingMinor - before).to.eql(99900));"
        ]
       }
      }
     ]
    },
    {
     "name": "A different merchant sees their own money, never yours (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/balances",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "balances"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ],
      "description": "TENANT ISOLATION, AND THE REQUEST CANNOT EVEN EXPRESS THE VIOLATION. There is no merchant parameter on this endpoint -- no path variable, no query string, no body. The tenant comes from the verified token, so the only balance a caller can address is their own.\n\nThat is why this returns 200 with someone else's (empty or unrelated) figures rather than 403: there is nothing to forbid."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const inr = pm.response.json().balances.find(r => r.currency === 'INR');",
         "const mine = Number(pm.collectionVariables.get('lgBalanceBefore')) + 99900;",
         "pm.test('not this merchant\\'s balance', () =>",
         "    pm.expect(inr ? inr.pendingMinor : 0).to.not.eql(mine));"
        ]
       }
      }
     ]
    },
    {
     "name": "No token, no balance (401)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/balances",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "balances"
       ]
      },
      "header": [],
      "description": "Nothing under /api/v1 is open by accident. The balance endpoint needed no security configuration of its own: everything not explicitly permitted is authenticated, and adding a rule for this route would be a no-op implying the default runs the other way."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Refund (money back out, ADR-019)",
   "description": "GIVING IT BACK, AND THE LAST PHASE-1 CAPABILITY.\n\nCollects 99900, refunds 30000 of it, and watches three modules move without any of them calling another: Refund announces refund.succeeded, the Ledger posts a REVERSAL journal, and Payment reaches PARTIALLY_REFUNDED. Then refunds the remainder.\n\nTHE DANGER HERE IS THE MIRROR OF PAYMENT'S. Payment's is collecting twice; Refund's is paying out more than was collected -- reachable by ordinary concurrency, since two partial refunds sent at the same moment each read a total that excludes the other. A row lock on the payment is what settles that; a deferred trigger backs it up for anything reaching the table another way. ADR-019 section 4 explains why the trigger alone is NOT enough, which was found by measuring rather than by reasoning.\n\nA REVERSAL IS A NEW JOURNAL, NEVER AN EDIT. Ledger entries refuse UPDATE and DELETE outright, so a correction can only be another posting in the opposite direction. Both stay in the history.\n\nTHE REFUND CALLBACK IS HAND-SIGNED, and it is the only such request left in this collection. The simulator can queue payment callbacks but not refund ones -- its outbound-callback model is shaped around payments.\n\nSTART THE APP WITH BOTH TIMERS ON:\n    PAYMESH_SIMULATOR_DISPATCH_ENABLED=true \\\n    PAYMESH_EVENTS_OUTBOX_RELAY_ENABLED=true \\\n    ./mvnw spring-boot:run\n\nRuns after the Ledger folder and reuses its access tokens.",
   "item": [
    {
     "name": "Setup: an order to collect and then give back (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 99900,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rfOrderKey}}"
       }
      ],
      "description": "Its own order and its own payment. Refunds are the last folder and everything above has already spent its intents (ADR-011: one live intent per order)."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "['rfOrderKey','rfIntentKey','rfAttachKey','rfConfirmKey','rfRefundKey','rfPartialKey',",
         " 'rfOverKey','rfCancelKey','rfCrossKey'].forEach(k =>",
         "    pm.collectionVariables.set(k, pm.variables.replaceIn('{{$guid}}')));",
         "pm.collectionVariables.set('refundCallbackSecret', 'dev-only-insecure-refund-callback-secret-change-me');",
         "pm.collectionVariables.set('rfPollAttempt', 0);",
         "// This folder's OWN baseline, read below before anything is collected. Deriving it",
         "// from the Ledger folder's variable would make this folder fail whenever it is run",
         "// on its own -- and a folder that only passes in sequence is a folder nobody trusts."
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('rfOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Baseline: the balance before this folder does anything (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/balances",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "balances"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Read first so the assertions later are about a DELTA this folder caused, not about an absolute figure that depends on which folders ran before it."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "setTimeout(() => {}, 1500);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// SETTLE BEFORE MEASURING. Every folder shares one merchant, and the relay moves that",
         "// merchant's balance asynchronously -- so an earlier folder's collection can land in the",
         "// middle of this one and be counted as ours. Poll until two consecutive reads agree, which",
         "// means the backlog is drained, and only then record the baseline.",
         "//",
         "// Without this the delta assertions below measure whatever happened to be in flight. That is",
         "// exactly how they failed the first time this collection was run end to end.",
         "const inr = pm.response.json().balances.find(r => r.currency === 'INR');",
         "const now = inr ? inr.pendingMinor : 0;",
         "const last = pm.collectionVariables.get('rfSettleLast');",
         "const tries = Number(pm.collectionVariables.get('rfSettleTries') || 0) + 1;",
         "",
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "",
         "if ((last === undefined || last === '' || Number(last) !== now) && tries < 20) {",
         "    pm.collectionVariables.set('rfSettleLast', now);",
         "    pm.collectionVariables.set('rfSettleTries', tries);",
         "    postman.setNextRequest(pm.info.requestName);",
         "} else {",
         "    pm.collectionVariables.set('rfBalanceBefore', now);",
         "    console.log('settled baseline: ' + now);",
         "}"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: intent, attach, confirm (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{rfOrderId}}\",\n  \"amountMinor\": 99900,\n  \"currency\": \"INR\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rfIntentKey}}"
       }
      ],
      "description": ""
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('rfIntentId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach a card (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{rfIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{rfIntentId}}",
        "payment-method"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rfAttachKey}}"
       }
      ],
      "description": "The Idempotency-Key is REQUIRED -- attach is a registered idempotent route."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: confirm (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{rfIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{rfIntentId}}",
        "confirm"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rfConfirmKey}}"
       }
      ],
      "description": ""
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('PROCESSING', () => pm.expect(pm.response.json().status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: the simulator collects the full 99900 (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{rfOrderKey}}-collect\",\n  \"callbackReference\": \"{{rfIntentId}}\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 99900,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "There has to be money before there can be a refund. tok_sim_success, full amount."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));"
        ]
       }
      }
     ]
    },
    {
     "name": "Wait for the payment to reach SUCCEEDED (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{rfIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{rfIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Polls: the simulator's dispatcher and the outbox relay both have to tick. Needs both timers on -- see the folder description."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "setTimeout(() => {}, 2000);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const attempt = Number(pm.collectionVariables.get('rfPollAttempt') || 0) + 1;",
         "pm.collectionVariables.set('rfPollAttempt', attempt);",
         "const b = pm.response.json();",
         "if (b.status !== 'SUCCEEDED' && attempt < 15) {",
         "    console.log('waiting for the collection, attempt ' + attempt + ' of 15');",
         "    postman.setNextRequest(pm.info.requestName);",
         "} else {",
         "    pm.test('SUCCEEDED', () => pm.expect(b.status).to.eql('SUCCEEDED'));",
         "    pm.collectionVariables.set('rfPollAttempt', 0);",
         "}"
        ]
       }
      }
     ]
    },
    {
     "name": "Refuse a refund larger than what was captured (422)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/refunds",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "refunds"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentIntentId\": \"{{rfIntentId}}\",\n  \"amountMinor\": 100000\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rfOverKey}}"
       }
      ],
      "description": "THE RULE THE MODULE EXISTS FOR, in its readable form. The message names the requested amount, what is already spoken for, and what was captured -- all three, because 'too much' without the figures leaves a merchant guessing at what would fit.\n\nThis is the application's pre-check. It is NOT the guard: two simultaneous refunds each read a total that excludes the other and both would pass. A row lock on the payment is what settles that, and a deferred trigger backs it up for anything that reaches the table another way (ADR-019 section 4)."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Entity', () => pm.response.to.have.status(422));",
         "pm.test('REFUND_EXCEEDS_CAPTURED_AMOUNT', () =>",
         "    pm.expect(pm.response.json().code).to.eql('REFUND_EXCEEDS_CAPTURED_AMOUNT'));",
         "pm.test('names the captured figure', () =>",
         "    pm.expect(pm.response.json().message).to.include('99900'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refund 30000 of it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/refunds",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "refunds"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentIntentId\": \"{{rfIntentId}}\",\n  \"amountMinor\": 30000,\n  \"reason\": \"Customer returned one item\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rfPartialKey}}"
       }
      ],
      "description": "A partial refund. Created and handed to the provider in one transaction, so it comes back PROCESSING rather than PENDING.\n\nNOTE THERE IS NO CURRENCY FIELD. A refund is denominated in whatever the payment collected, so there is nothing for a caller to get wrong -- and no way to ask for 30000 JPY back from an INR capture. A database trigger enforces the same thing."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.collectionVariables.set('rfRefundId', b.id);",
         "pm.test('ref_ prefixed id', () => pm.expect(b.id).to.match(/^ref_/));",
         "pm.test('PROCESSING', () => pm.expect(b.status).to.eql('PROCESSING'));",
         "pm.test('denominated in the payment currency', () => pm.expect(b.currency).to.eql('INR'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refuse a cancel once the provider has it (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/refunds/{{rfRefundId}}/cancel",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "refunds",
        "{{rfRefundId}}",
        "cancel"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rfCancelKey}}"
       }
      ],
      "description": "SDD 16.3 asks for a cancel endpoint and this is it answering honestly. A refund is PROCESSING by the time the caller sees it, and PROCESSING means the provider may already have moved the money -- reporting CANCELLED then would be PayMesh's opinion contradicting the customer's bank statement.\n\nThe endpoint's real use is clearing a refund that failed to submit and is holding its amount against the captured total."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('REFUND_NOT_CANCELLABLE', () =>",
         "    pm.expect(pm.response.json().code).to.eql('REFUND_NOT_CANCELLABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refuse an unsigned refund callback (401)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/refund-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "refund-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"eventId\": \"unsigned\",\n  \"occurredAt\": \"2026-08-02T12:00:00Z\",\n  \"refundId\": \"{{rfRefundId}}\",\n  \"outcome\": \"SUCCEEDED\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "description": "REFUND'S OWN CALLBACK ROUTE (ADR-019), not a branch of the payment one. It is permitAll on the Spring chain because a provider holds no PayMesh token -- the HMAC signature is the whole authentication, and it must not be reachable with a merchant's token either, because this route decides that money went back.\n\nSame filter class as the payment route, a separate instance with its own secret. Two instances of one implementation, not two implementations."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));",
         "pm.test('and says nothing about which check failed', () =>",
         "    pm.expect(pm.response.json().code).to.eql('PROVIDER_SIGNATURE_INVALID'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The provider says the refund succeeded (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/refund-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "refund-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{refundCallbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{refundCallbackSignature}}"
       }
      ],
      "description": "THE SIGNATURE IS COMPUTED IN THE PRE-REQUEST SCRIPT, not pasted -- a fixed one would be valid for one body at one second.\n\nThis is the one hand-signed request left in the collection. The simulator can queue payment callbacks but not refund ones: its outbound-callback model is shaped around payments, and closing that is its own migration (ADR-019)."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const body = JSON.stringify({",
         "    eventId: 'sim_ref_evt_' + pm.variables.replaceIn('{{$guid}}'),",
         "    occurredAt: new Date().toISOString(),",
         "    refundId: pm.collectionVariables.get('rfRefundId'),",
         "    providerReference: 'prov_re_' + pm.variables.replaceIn('{{$guid}}'),",
         "    outcome: 'SUCCEEDED'",
         "});",
         "const t = Math.floor(Date.now() / 1000);",
         "// t + '.' + body, so a captured signature cannot be replayed onto a different body or time.",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('refundCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "// The request body is literally {{refundCallbackBody}}, so what is sent is byte-for-byte what",
         "// was signed. Building the JSON in the request pane would sign one string and send another.",
         "pm.collectionVariables.set('refundCallbackBody', body);",
         "pm.collectionVariables.set('refundCallbackSignature', 't=' + t + ',v1=' + v1);",
         "pm.collectionVariables.set('rfPollAttempt', 0);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('APPLIED', () => pm.expect(pm.response.json().result).to.eql('APPLIED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The same callback again changes nothing (200 DUPLICATE)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/internal/v1/refund-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "refund-callbacks",
        "SIMULATOR"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{{refundCallbackBody}}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Signature",
        "value": "{{refundCallbackSignature}}"
       }
      ],
      "description": "Deduplicated on (provider, external_event_id), which is the PRIMARY KEY of refund_callbacks rather than a unique index consulted by a read -- two concurrent deliveries of one callback both find nothing on a read, and only the index can decide between them.\n\nStill a 200: a duplicate is not the provider's error, and answering 4xx would make a correct provider retry forever."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// The SAME body and a fresh timestamp: the signature covers t + '.' + body, so re-signing is",
         "// required, but the eventId inside the body is unchanged -- which is what makes it a duplicate.",
         "const body = pm.collectionVariables.get('refundCallbackBody');",
         "const t = Math.floor(Date.now() / 1000);",
         "const v1 = CryptoJS.HmacSHA256(",
         "    t + '.' + body,",
         "    pm.collectionVariables.get('refundCallbackSecret')",
         ").toString(CryptoJS.enc.Hex);",
         "pm.collectionVariables.set('refundCallbackSignature', 't=' + t + ',v1=' + v1);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('DUPLICATE', () => pm.expect(pm.response.json().result).to.eql('DUPLICATE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "THE POINT: the balance drops and the payment reads PARTIALLY_REFUNDED (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/balances",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "balances"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "THREE MODULES MOVED AND NONE OF THEM CALLED ANOTHER. Refund announced refund.succeeded; the Ledger posted a REVERSAL journal because it subscribed; Payment moved its own status because it subscribed too.\n\nThe reversal is a NEW journal, not an edit -- ledger entries refuse UPDATE and DELETE outright, so a correction can only ever be another posting in the opposite direction:\n\n    capture:   DEBIT provider-clearing    CREDIT merchant-pending\n    reversal:  DEBIT merchant-pending     CREDIT provider-clearing\n\nBoth stay in the history. 99900 collected minus 30000 given back leaves 69900."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "setTimeout(() => {}, 2000);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const attempt = Number(pm.collectionVariables.get('rfPollAttempt') || 0) + 1;",
         "pm.collectionVariables.set('rfPollAttempt', attempt);",
         "",
         "const inr = pm.response.json().balances.find(r => r.currency === 'INR');",
         "const before = Number(pm.collectionVariables.get('rfBalanceBefore'));",
         "const delta = (inr ? inr.pendingMinor : 0) - before;",
         "",
         "// 99900 collected minus 30000 given back. Asserted as a DELTA so the folder does",
         "// not care what any earlier folder left behind.",
         "if (delta !== 69900 && attempt < 15) {",
         "    console.log('waiting for the reversal, delta ' + delta + ', attempt ' + attempt + ' of 15');",
         "    postman.setNextRequest(pm.info.requestName);",
         "} else {",
         "    pm.test('collected 99900, gave back 30000, balance rose by 69900', () =>",
         "        pm.expect(delta).to.eql(69900));",
         "}"
        ]
       }
      }
     ]
    },
    {
     "name": "The payment now reads PARTIALLY_REFUNDED (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{rfIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{rfIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "A STATUS THAT WAS UNREACHABLE UNTIL THIS CAPABILITY EXISTED. PARTIALLY_REFUNDED and REFUNDED have been declared since V8 and produced by nothing -- project-status.md verified it by grep. Every status in the payment enum is now reachable.\n\nPayment moved its own column because Refund may not write it, exactly as Payment may not write orders.status."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('PARTIALLY_REFUNDED', () => pm.expect(b.status).to.eql('PARTIALLY_REFUNDED'));",
         "pm.test('refundedAmountMinor moved with it', () =>",
         "    pm.expect(b.refundedAmountMinor).to.eql(30000));"
        ]
       }
      }
     ]
    },
    {
     "name": "Read the refund back (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/refunds/{{rfRefundId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "refunds",
        "{{rfRefundId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": ""
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('SUCCEEDED', () => pm.expect(b.status).to.eql('SUCCEEDED'));",
         "pm.test('carries the provider reference', () =>",
         "    pm.expect(b.providerReference).to.be.a('string'));"
        ]
       }
      }
     ]
    },
    {
     "name": "List this merchant's refunds (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/refunds",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "refunds"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Keyset pagination, newest first, same shape as orders and payment intents. Scoped to the caller's merchant -- not filtered."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('contains the refund just made', () =>",
         "    pm.expect(pm.response.json().data.map(r => r.id)).to.include(",
         "        pm.collectionVariables.get('rfRefundId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Another merchant cannot see it (404)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/refunds/{{rfRefundId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "refunds",
        "{{rfRefundId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ],
      "description": "404, NOT 403. A 403 would confirm the refund exists and let anyone enumerate every refund on the platform by watching which ids answer which (ADR-007)."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('REFUND_NOT_FOUND', () =>",
         "    pm.expect(pm.response.json().code).to.eql('REFUND_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Another merchant cannot refund this payment either (422)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/refunds",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "refunds"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentIntentId\": \"{{rfIntentId}}\",\n  \"amountMinor\": 100\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rfCrossKey}}"
       }
      ],
      "description": "And the answer is byte-for-byte what a payment that never existed returns. Three causes -- no such payment, not yours, collected nothing -- one code, so nothing is learned from the difference."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Entity', () => pm.response.to.have.status(422));",
         "pm.test('PAYMENT_NOT_REFUNDABLE', () =>",
         "    pm.expect(pm.response.json().code).to.eql('PAYMENT_NOT_REFUNDABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refund the rest -- no amount means what is LEFT (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/refunds",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "refunds"
       ]
      },
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentIntentId\": \"{{rfIntentId}}\"\n}"
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rfRefundKey}}"
       }
      ],
      "description": "NO amountMinor, which means 'refund what is LEFT' -- 69900, not the 99900 captured. Reading it as the captured total would fail the over-refund check every time against a partly-refunded payment, making the convenience useless exactly when it is most wanted.\n\nIT STOPS AT PROCESSING, deliberately. Taking this one to SUCCEEDED needs a second hand-signed callback, and the point here is the amount rather than the loop -- the loop is proved above. The payment reaching REFUNDED once both refunds settle is covered by RefundIntegrationTest.accumulatesPartialRefundsToRefunded."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('the remainder, not the whole capture', () =>",
         "    pm.expect(b.amountMinor).to.eql(69900));",
         "pm.test('PROCESSING -- nothing has settled it yet', () =>",
         "    pm.expect(b.status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Outbox health (the ADR-025 alert)",
   "description": "THE ALERT SDD SECTION 24 ASKS FOR, ON THE ENDPOINT ANYTHING WATCHING THIS APPLICATION ALREADY POLLS.\n\nUntil ADR-025 a failing event was retried at the head of every pass forever, and its aggregate's later events were deferred behind it forever -- visible only as one WARN per pass. There was no dead letter, no attempt counter and no alert, and open item 14 called it the largest hole in event delivery.\n\nTwo numbers close it, and both are here:\n\n- oldestUnpublishedAgeSeconds -- SDD section 24's own metric, and the ONLY thing that distinguishes 'the relay is keeping up' from 'the relay stopped an hour ago'. A relay whose timer is off is otherwise completely silent: no errors, no logs, nothing. Only the age moves.\n- deadLetteredEvents -- committed state changes no consumer will ever hear about. This does not heal on its own, and the ERROR the relay logged when it gave up scrolled past days ago.\n\nWHY NOT PROMETHEUS: SDD section 26 wants a metrics stack and none exists. Reading 'add an alert' as 'first build an observability stack' is part of why item 14 stood unclosed. /actuator/health was already exposed and is already what a watcher polls; a gauge nobody has written a rule for is not an alert either.\n\nDETAILS ARE VISIBLE BECAUSE THIS IS THE dev PROFILE. application.yaml sets show-details: never and production keeps it -- component internals are not for unauthenticated callers. The dev profile overrides it, because these two numbers ARE the alert and hiding them leaves a bare UP/DOWN.",
   "item": [
    {
     "name": "The outbox backlog is reported and healthy (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/actuator/health",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "actuator",
        "health"
       ]
      },
      "header": [],
      "description": "No authentication: this is the endpoint an orchestrator and an alerting scraper reach, neither of which holds a merchant token.\n\nThe component name is derived by Boot from the bean name with the HealthIndicator suffix stripped, so it is outboxBacklog. Renaming that bean silently renames what anything watching this keys on -- OutboxHealthEndpointTest pins it for exactly that reason."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "",
         "pm.test('the outbox backlog is a reported component', () =>",
         "    pm.expect(b.components).to.have.property('outboxBacklog'));",
         "",
         "const outbox = b.components.outboxBacklog;",
         "",
         "pm.test('SDD 24 metric: the age of the oldest undelivered event', () =>",
         "    pm.expect(outbox.details).to.have.property('oldestUnpublishedAgeSeconds'));",
         "pm.test('and how many the relay has permanently given up on', () =>",
         "    pm.expect(outbox.details).to.have.property('deadLetteredEvents'));",
         "",
         "// Named separately because they need DIFFERENT responses: a stalled backlog is usually",
         "// 'the relay is not running'; an abandoned event is always 'fix the consumer, then requeue'.",
         "// Collapsing them into one boolean costs the first ten minutes of an incident.",
         "pm.test('the two failures are distinguishable', () => {",
         "    pm.expect(outbox.details).to.have.property('backlogStalled');",
         "    pm.expect(outbox.details).to.have.property('eventsAbandoned');",
         "});",
         "",
         "pm.test('nothing has been abandoned on a healthy platform', () => {",
         "    pm.expect(outbox.details.eventsAbandoned).to.eql(false);",
         "    pm.expect(outbox.status).to.eql('UP');",
         "});",
         "",
         "console.log('oldest undelivered event: ' + outbox.details.oldestUnpublishedAgeSeconds +",
         "            's, threshold ' + outbox.details.alertAgeSeconds + 's');"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Reconciliation (the provider's record repairs PayMesh, ADR-026)",
   "description": "THE JOB ADR-015 NAMED AND NOBODY WROTE, END TO END.\n\nGET /sim/v1/reconciliation/{date} has produced the provider's daily truth since ADR-017 and NOTHING READ IT. Three shipped pieces of code leaned on the job that did not exist: ADR-015 fails a stranded payment with no evidence it failed and names reconciliation as the mitigation; ADR-023 does the same for refunds and says outright the real answer 'does not exist yet'; ADR-019 lists a lost refund callback as Refund's known gap.\n\nThe cost was never an untidy row. When the provider had actually collected, PayMesh held a FAILED payment, the Ledger never posted, and the merchant was short -- permanently, and silently.\n\nWHAT THIS FOLDER REPRODUCES, in order: a payment is confirmed, the provider collects it in full, the callback is never delivered, PayMesh sits in PROCESSING knowing nothing -- and then reconciliation reads the provider's own record and puts it right, all the way through to the order reaching PAID and the Ledger posting the balance.\n\nIT REPLAYS, IT DOES NOT DIFF. There is no comparison against PayMesh's state anywhere in the job. Every terminal provider row is handed to the SAME callback service a real callback goes through, and the aggregate decides. So the amount check, the staleness guard, the attempt row and the outbox event that makes the Ledger post are all the existing ones -- a job that re-derived any of them would be a second copy of the transition rules on the money path.\n\nRUN THE APP LIKE THIS, or the last two requests exhaust their polls:\n\n    PAYMESH_RECONCILIATION_ENABLED=true \\\n    PAYMESH_RECONCILIATION_INITIAL_DELAY=3s \\\n    PAYMESH_RECONCILIATION_INTERVAL=5s \\\n    PAYMESH_EVENTS_OUTBOX_RELAY_ENABLED=true \\\n    ./mvnw spring-boot:run\n\nLEAVE THE SIMULATOR'S DISPATCHER OFF (the dev default). It is what makes the callback 'lost'; with it on, the payment succeeds normally and there is no divergence to repair.",
   "item": [
    {
     "name": "Setup: an order the provider will collect and never mention (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rcOrderKey}}"
       }
      ],
      "description": "Its own order. An order holds at most one live payment intent (ADR-011), so every folder above has already spent the ones it created.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "['rcOrderKey','rcIntentKey','rcAttachKey','rcConfirmKey','rcSimKey','rcRefundSimKey']",
         "    .forEach(k => pm.collectionVariables.set(k, pm.variables.replaceIn('{{$guid}}')));",
         "pm.collectionVariables.set('simulatorKey', 'dev-only-insecure-simulator-api-key-change-me');",
         "",
         "// The export is keyed on the provider row's created_at, as a UTC day.",
         "pm.collectionVariables.set('rcToday', new Date().toISOString().slice(0, 10));",
         "",
         "// The poll below counts its own attempts. Reset so a second run of the folder does not",
         "// start already exhausted.",
         "pm.collectionVariables.set('rcPollAttempt', 0);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('rcOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a payment intent for it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rcIntentKey}}"
       }
      ],
      "description": "Ordinary creation. Nothing about this intent is special -- which is the point: the divergence below happens to an entirely normal payment.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{rcOrderId}}\",\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('rcIntentId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach a payment method (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{rcIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{rcIntentId}}",
        "payment-method"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rcAttachKey}}"
       }
      ],
      "description": "",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: confirm it, reaching PROCESSING (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{rcIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{rcIntentId}}",
        "confirm"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{rcConfirmKey}}"
       }
      ],
      "description": "PROCESSING means PayMesh has handed the collection to a provider and is waiting to be told what happened. Everything that follows is about that answer never arriving.",
      "body": {
       "mode": "raw",
       "raw": "{}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.test('PROCESSING', () => pm.expect(pm.response.json().status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "THE PROVIDER COLLECTS THE MONEY (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "tok_sim_success on AUTOMATIC capture: the provider takes the payment IN FULL and queues a callback saying so.\n\ncallbackReference carries PayMesh's payment intent id. The simulator never parses it -- it is an opaque string it stores and echoes -- but it is the only thing that will let the reconciliation export be joined back to PayMesh. That is the whole reason V13 gave provider_payments the column, and V22 gave provider_refunds the same one.\n\nTHE CALLBACK IS QUEUED AND NEVER DELIVERED, because the dev profile turns the simulator's dispatcher off (PAYMESH_SIMULATOR_DISPATCH_ENABLED). From PayMesh's side that is indistinguishable from a callback lost in transit -- which is exactly the failure being reproduced.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{rcSimKey}}\",\n  \"callbackReference\": \"{{rcIntentId}}\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 4000,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('the provider captured the full amount', () => {",
         "    pm.expect(b.status).to.eql('CAPTURED');",
         "    pm.expect(b.capturedAmountMinor).to.eql(4000);",
         "});",
         "pm.test('and it echoed our reference back', () =>",
         "    pm.expect(b.callbackReference).to.eql(pm.collectionVariables.get('rcIntentId')));",
         "pm.collectionVariables.set('rcSimPaymentId', b.providerPaymentId);"
        ]
       }
      }
     ]
    },
    {
     "name": "THE DIVERGENCE: PayMesh still says PROCESSING (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{rcIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{rcIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The money has moved and PayMesh does not know. This is the state ADR-015's sweeper exists for, and the state it will eventually resolve BY GUESSING: after an hour it writes FAILED with failureCode provider_no_response, and its own failure message admits the attempt 'may still have succeeded at the provider and needs reconciliation'.\n\nLeft alone, that guess used to be final. The Ledger never posted, and the merchant was simply short -- permanently, and silently."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('PayMesh has not been told, so it is still waiting', () =>",
         "    pm.expect(pm.response.json().status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The provider's own record says CAPTURED (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/reconciliation/{{rcToday}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "reconciliation",
        "{{rcToday}}"
       ]
      },
      "header": [
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "ONE UTC DAY OF THE PROVIDER'S TRUTH, AND UNTIL ADR-026 NOTHING READ IT. This export has existed since ADR-017, whose own javadoc says 'This is the input. It is not the job.'\n\nWhat makes it usable is the last assertion below: the row carries callbackReference, so it can be joined back to a PayMesh payment intent. Without that a row names a provider payment and nothing PayMesh recognises -- which is precisely why V22 had to add the same column to provider_refunds before refunds could be reconciled at all.\n\ncountsByStatus carries TIMED_OUT as well: those are payments the provider decided and never reported, and on this provider they carry capturedAmountMinor 0 -- so replaying one as FAILED turns ADR-015's guess into a confirmation rather than a second guess."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "const mine = b.payments.find(p => p.providerPaymentId === pm.collectionVariables.get('rcSimPaymentId'));",
         "",
         "pm.test('our payment is in the day the provider reports', () =>",
         "    pm.expect(mine, 'payment missing from the export').to.not.be.undefined);",
         "pm.test('the provider says it collected 4000', () => {",
         "    pm.expect(mine.status).to.eql('CAPTURED');",
         "    pm.expect(mine.capturedAmountMinor).to.eql(4000);",
         "});",
         "pm.test('AND THE ROW CAN BE JOINED BACK TO PayMesh -- without this the file is unusable', () =>",
         "    pm.expect(mine.callbackReference).to.eql(pm.collectionVariables.get('rcIntentId')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Reconciliation repairs it, and PayMesh reaches SUCCEEDED (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{rcIntentId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{rcIntentId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "THE POINT OF THIS FOLDER. Nobody sent a callback and nobody called a payment endpoint: the job read the provider's file, built the same ProviderEvent the HTTP boundary builds, and handed it to the same RecordProviderCallbackService a real callback goes through (ADR-026). The aggregate decided; the job did not.\n\nIT POLLS, because the job runs on a timer.\n\nREQUIRES RECONCILIATION TO BE ON AND FAST. The dev profile switches it off so it cannot repair rows underneath the test suite, and its production interval is an hour. Start the app with:\n\n    PAYMESH_RECONCILIATION_ENABLED=true \\\n    PAYMESH_RECONCILIATION_INITIAL_DELAY=3s \\\n    PAYMESH_RECONCILIATION_INTERVAL=5s \\\n    ./mvnw spring-boot:run\n\nWithout those this request exhausts its attempts and fails, which is the honest outcome rather than a skip."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// The reconciliation interval suggested in the description is 5s.",
         "setTimeout(() => {}, 2000);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const attempt = Number(pm.collectionVariables.get('rcPollAttempt') || 0) + 1;",
         "pm.collectionVariables.set('rcPollAttempt', attempt);",
         "",
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "",
         "if (b.status !== 'SUCCEEDED' && attempt < 12) {",
         "    console.log('waiting for reconciliation, attempt ' + attempt + ' of 12 (status ' + b.status + ')');",
         "    postman.setNextRequest(pm.info.requestName);",
         "} else {",
         "    pm.test('SUCCEEDED -- repaired from the provider\\'s own record', () =>",
         "        pm.expect(b.status).to.eql('SUCCEEDED'));",
         "    pm.test('for the amount the provider actually collected', () =>",
         "        pm.expect(b.capturedAmountMinor).to.eql(4000));",
         "    pm.test('and it does not still claim a reason for failing', () =>",
         "        pm.expect(b.failureCode == null, 'a repaired payment kept its stale failureCode').to.be.true);",
         "}"
        ]
       }
      }
     ]
    },
    {
     "name": "The order reaches PAID, so the repair is not cosmetic (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders/{{rcOrderId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders",
        "{{rcOrderId}}"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "A repair that moved a status column and not the money would be worse than none.\n\nBecause the repair went through the ordinary callback path, it wrote payment.succeeded to outbox_events in the same transaction (ADR-010). The relay delivers it, Order's consumer moves the order, and the Ledger's consumer posts a balanced journal -- none of which reconciliation knows exists. That is what replaying rather than diffing buys.\n\nALSO NEEDS THE RELAY: PAYMESH_EVENTS_OUTBOX_RELAY_ENABLED=true."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "setTimeout(() => {}, 1500);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const attempt = Number(pm.collectionVariables.get('rcPollAttempt') || 0) + 1;",
         "pm.collectionVariables.set('rcPollAttempt', attempt);",
         "",
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "",
         "if (b.status !== 'PAID' && attempt < 20) {",
         "    console.log('waiting for the relay, attempt ' + attempt + ' of 20');",
         "    postman.setNextRequest(pm.info.requestName);",
         "} else {",
         "    pm.test('PAID', () => pm.expect(b.status).to.eql('PAID'));",
         "    pm.test('and the amount followed the money', () =>",
         "        pm.expect(b.amountPaidMinor).to.eql(4000));",
         "}"
        ]
       }
      }
     ]
    },
    {
     "name": "A refund also carries a reference now (201, ADR-026 / V22)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/refunds",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "refunds"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "provider_payments has had callback_reference since V13. provider_refunds never did, because SimulatedRefund was built when PayMesh had no refund receiver at all.\n\nThe consequence was not an untidy schema: HALF THE PROVIDER'S DAILY TRUTH WAS UNUSABLE. A refund row in the export named a provider refund and a provider payment and nothing PayMesh recognised, so no job could repair a refund whose callback was lost -- the gap ADR-019 recorded and ADR-023's sweeper could only guess at.\n\nNullable and staying nullable: rows written before V22 have none, and inventing one would be fabricating provider data. A refund with no reference is simply one reconciliation reports as unmatched.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{rcRefundSimKey}}\",\n  \"providerPaymentId\": \"{{rcSimPaymentId}}\",\n  \"callbackReference\": \"ref_reconciliation_demo\",\n  \"amountMinor\": 1000\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "pm.test('the provider sent the money back', () => pm.expect(b.status).to.eql('SUCCEEDED'));",
         "pm.test('and echoed the caller\\'s reference, which is what makes it joinable', () =>",
         "    pm.expect(b.callbackReference).to.eql('ref_reconciliation_demo'));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Webhook (PayMesh calls the merchant back, ADR-028)",
   "description": "THE FIRST CAPABILITY THAT POINTS OUTWARD. Everything above this folder is PayMesh answering a request. This is PayMesh making one -- signed, retried, and aimed at a URL the merchant chose.\n\nTHE SECRET IS NEVER STORED. webhook_endpoints holds secret_version, an integer. The signing secret is HMAC-SHA256(masterKey, \"paymesh.webhook.v1|<endpointId>|<version>\" || 0x01), derived on demand, so a database dump contains nothing that lets an attacker sign as PayMesh. Rotation is an increment (ADR-028 section 2).\n\nTWO CONSEQUENCES YOU CAN SEE BELOW. The secret is shown exactly twice -- on create and on rotate -- and neither route is on the idempotency filter, because idempotency_records.response_body stores response bodies VERBATIM and registering them would write the secret to the database in cleartext. Rotate is idempotent on its own terms instead: it names the version it is replacing, so a retry re-derives the same secret rather than bumping again. That is the request called 'idempotent by construction' below, and it is the whole argument in one call.\n\nA MERCHANT ENDPOINT BEING DOWN MUST NEVER AFFECT A PAYMENT. No HTTP happens on the payment path: the event dispatcher writes one webhook_events row and one PENDING delivery per subscribed endpoint, inside the transaction it was already in, and a separate timer does the sending.\n\nSTART THE APP WITH BOTH TIMERS ON, or nothing is ever queued:\n    PAYMESH_SIMULATOR_DISPATCH_ENABLED=true \\\n    PAYMESH_EVENTS_OUTBOX_RELAY_ENABLED=true \\\n    ./mvnw spring-boot:run\nThe webhook DISPATCHER is deliberately left off. It would POST at https://merchant.test/, which does not exist, and every delivery below would spend its retry budget against DNS. PENDING is the correct and observable state for a walkthrough; DeliverWebhooksServiceTest and HttpWebhookSenderTest cover the sending.",
   "item": [
    {
     "name": "Register a webhook endpoint, and read the secret once (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "NO Idempotency-Key, and its absence is the design rather than an omission. This response carries the signing secret, and the idempotency filter persists response bodies verbatim so a retry can replay them -- registering this route would put the secret in the database in cleartext, which is exactly what ADR-028 exists to avoid.\n\nThe cost is stated rather than hidden: a merchant who retries a lost create gets a second endpoint. They can see it and disable it, and two endpoints at one URL are refused outright by the next request.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"url\": \"https://merchant.test/{{whSlug}}/hooks\",\n  \"subscriptions\": [\n    \"order.paid\",\n    \"payment.succeeded\"\n  ]\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// A fresh URL per run, so the folder can be re-run against the same app. The unique is",
         "// (merchant_id, url) and a fixed URL would 409 on the second run.",
         "pm.collectionVariables.set('whSlug', pm.variables.replaceIn('{{$guid}}'));",
         "['whOrderKey','whIntentKey','whAttachKey','whConfirmKey','whReplayKey'].forEach(k => pm.collectionVariables.set(k, pm.variables.replaceIn('{{$guid}}')));",
         "pm.collectionVariables.set('whPollAttempt', 0);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "const b = pm.response.json();",
         "",
         "pm.test('the id is opaque and prefixed (ADR-003)', () =>",
         "    pm.expect(b.id).to.match(/^whe_[0-9a-f-]{36}$/));",
         "pm.test('the secret is shown, once, and is prefixed', () =>",
         "    pm.expect(b.secret).to.match(/^pmsec_[A-Za-z0-9_-]{43}$/));",
         "pm.test('version 1, because rotation is an integer bump', () =>",
         "    pm.expect(b.secretVersion).to.eql(1));",
         "pm.test('ACTIVE', () => pm.expect(b.status).to.eql('ACTIVE'));",
         "pm.test('no rotation window is open yet', () =>",
         "    pm.expect(b.previousSecretExpiresAt).to.be.null);",
         "",
         "pm.collectionVariables.set('whEndpointId', b.id);",
         "pm.collectionVariables.set('whSecret', b.secret);"
        ]
       }
      }
     ]
    },
    {
     "name": "The same URL twice is refused (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Two endpoints at one URL fan out twice to the same place: the merchant's handler sees every event doubled, and their own idempotency is the only thing between that and a doubled fulfilment.\n\nuq_webhook_endpoints_merchant_url is what makes it true. The service's pre-check is what makes it a readable 409 instead of a 500 -- two concurrent registrations can both pass the check, and the loser still gets this answer.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"url\": \"https://merchant.test/{{whSlug}}/hooks\",\n  \"subscriptions\": [\n    \"order.paid\"\n  ]\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('WEBHOOK_ENDPOINT_ALREADY_EXISTS', () =>",
         "    pm.expect(pm.response.json().code).to.eql('WEBHOOK_ENDPOINT_ALREADY_EXISTS'));"
        ]
       }
      }
     ]
    },
    {
     "name": "A plaintext http:// URL is refused (400)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The payload names an amount, a customer and an order. Over http it is readable by anything on the path, and the HMAC proves who sent it -- not that nobody read it.\n\nRefused in two places on purpose: ck_webhook_endpoints_url_https in V24 makes it unrepresentable, and the aggregate parses the URL because a regex cannot see what a parser can. https://user:pass@internal/ passes any prefix test and is refused here too.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"url\": \"http://merchant.test/insecure\",\n  \"subscriptions\": [\n    \"order.paid\"\n  ]\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));",
         "pm.test('INVALID_REQUEST', () =>",
         "    pm.expect(pm.response.json().code).to.eql('INVALID_REQUEST'));",
         "pm.test('and it says why', () =>",
         "    pm.expect(pm.response.json().message).to.include('https'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Subscribing to a type PayMesh does not publish is refused (422)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "payment.created is a real internal event -- it is on the outbox right now -- and no translator turns it into a merchant-facing shape, so an endpoint subscribed to it would sit ACTIVE and receive nothing forever. That is a configuration that looks correct and is not, which is worse than one that fails.\n\n422 rather than 400: the request is well-formed and the value is simply not one PayMesh offers. The message lists what it does offer, which is the useful half of the answer.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"url\": \"https://merchant.test/{{whSlug}}/never\",\n  \"subscriptions\": [\n    \"payment.created\"\n  ]\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('422 Unprocessable Entity', () => pm.response.to.have.status(422));",
         "pm.test('UNPUBLISHED_EVENT_TYPE', () =>",
         "    pm.expect(pm.response.json().code).to.eql('UNPUBLISHED_EVENT_TYPE'));",
         "pm.test('and it names the four that are available', () => {",
         "    const m = pm.response.json().message;",
         "    ['payment.succeeded','payment.failed','refund.succeeded','order.paid']",
         "        .forEach(t => pm.expect(m).to.include(t));",
         "});"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: an order to pay (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/orders",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "orders"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{whOrderKey}}"
       }
      ],
      "description": "Its own order: an order holds at most one live payment intent (ADR-011).",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"amountMinor\": 45000,\n  \"currency\": \"INR\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('whOrderId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: a payment intent against it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{whIntentKey}}"
       }
      ],
      "description": "AUTOMATIC capture, so the provider's SUCCEEDED callback collects in one hop.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"orderId\": \"{{whOrderId}}\",\n  \"amountMinor\": 45000,\n  \"currency\": \"INR\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));",
         "pm.collectionVariables.set('whIntentId', pm.response.json().id);"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: attach a card (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{whIntentId}}/payment-method",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{whIntentId}}",
        "payment-method"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{whAttachKey}}"
       }
      ],
      "description": "Attach is a registered idempotent route; without the header this is a 400.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"paymentMethodType\": \"CARD\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));"
        ]
       }
      }
     ]
    },
    {
     "name": "Setup: confirm, sending it to the provider (202)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/payment-intents/{{whIntentId}}/confirm",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "payment-intents",
        "{{whIntentId}}",
        "confirm"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{whConfirmKey}}"
       }
      ],
      "description": "PROCESSING. From here only a provider callback moves it.",
      "body": {
       "mode": "raw",
       "raw": "{}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('202 Accepted', () => pm.response.to.have.status(202));",
         "pm.test('PROCESSING', () => pm.expect(pm.response.json().status).to.eql('PROCESSING'));"
        ]
       }
      }
     ]
    },
    {
     "name": "The simulator collects it (201)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/sim/v1/payments",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "sim",
        "v1",
        "payments"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "X-PayMesh-Simulator-Key",
        "value": "{{simulatorKey}}"
       }
      ],
      "description": "The fake acquirer, which will queue a signed callback and let its own dispatcher deliver it. That callback moves the intent to SUCCEEDED, which writes payment.succeeded to the outbox, which the relay hands to every consumer -- Order, the Ledger, and now Webhook.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"idempotencyKey\": \"{{whSlug}}\",\n  \"callbackReference\": \"{{whIntentId}}\",\n  \"method\": \"CARD\",\n  \"token\": \"tok_sim_success\",\n  \"amountMinor\": 45000,\n  \"currency\": \"INR\",\n  \"captureMethod\": \"AUTOMATIC\"\n}"
      }
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.collectionVariables.set('simulatorKey',",
         "    'dev-only-insecure-simulator-api-key-change-me');"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('201 Created', () => pm.response.to.have.status(201));"
        ]
       }
      }
     ]
    },
    {
     "name": "THE POINT: a delivery is waiting, and nobody called a webhook endpoint to make it (200)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}/deliveries",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}",
        "deliveries"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "THE LINE THIS FOLDER EXISTS FOR. Payment wrote payment.succeeded and Order wrote order.paid; the relay handed both to their consumers; Webhook translated each into the merchant's shape and queued a delivery per subscribed endpoint. Payment and Order have never heard of Webhook.\n\nPENDING with attempts 0 is the CORRECT state here. The webhook dispatcher is off (see the folder description) because https://merchant.test/ does not resolve, and a walkthrough that burned a retry budget against DNS would prove less than this does.\n\nIT POLLS, because three hops have to happen first: the simulator's dispatcher, the callback, and the outbox relay. Both of those timers must be on."
     },
     "event": [
      {
       "listen": "prerequest",
       "script": {
        "type": "text/javascript",
        "exec": [
         "// A pre-request setTimeout is how a Postman run waits; the sandbox holds the request",
         "// until pending timers settle. Three hops, so longer than the Ledger folder's wait.",
         "setTimeout(() => {}, 2500);"
        ]
       }
      },
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "const attempt = Number(pm.collectionVariables.get('whPollAttempt') || 0) + 1;",
         "pm.collectionVariables.set('whPollAttempt', attempt);",
         "",
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const rows = pm.response.json();",
         "",
         "if (rows.length === 0 && attempt < 15) {",
         "    console.log('waiting for the callback and the relay, attempt ' + attempt + ' of 15');",
         "    postman.setNextRequest(pm.info.requestName);",
         "} else {",
         "    pm.test('a delivery was queued without anyone asking for one', () =>",
         "        pm.expect(rows.length).to.be.above(0));",
         "",
         "    const pending = rows.find(r => r.status === 'PENDING');",
         "",
         "    pm.test('it is PENDING and scheduled', () => {",
         "        pm.expect(pending, 'a PENDING delivery').to.not.be.undefined;",
         "        pm.expect(pending.nextAttemptAt).to.not.be.null;",
         "    });",
         "    pm.test('nothing has been attempted yet -- the dispatcher is off', () =>",
         "        pm.expect(pending.attempts).to.eql(0));",
         "    pm.test('the delivery id is opaque and prefixed', () =>",
         "        pm.expect(pending.id).to.match(/^whd_[0-9a-f-]{36}$/));",
         "",
         "    pm.collectionVariables.set('whDeliveryId', pending.id);",
         "}"
        ]
       }
      }
     ]
    },
    {
     "name": "Replaying a delivery the dispatcher will retry anyway is refused (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}/deliveries/{{whDeliveryId}}/replay",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}",
        "deliveries",
        "{{whDeliveryId}}",
        "replay"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Idempotency-Key",
        "value": "{{whReplayKey}}"
       }
      ],
      "description": "THE ONE WEBHOOK ROUTE ON THE IDEMPOTENCY FILTER, and the Idempotency-Key above is therefore required. Its response carries no secret, and a retried replay would otherwise collide with the PENDING state the first one produced and answer 409 -- the wrong answer to a network retry of a request that worked.\n\nThe 409 here is a different 409: this delivery has never been attempted, so replaying it would mean two POSTs to the merchant for one event -- the replay's and the retry the schedule already has queued. A merchant's endpoint is supposed to be idempotent, but 'we send duplicates and you deal with it' is not a contract PayMesh should write on purpose. Replay is for a delivery that is DELIVERED or FAILED.",
      "body": {
       "mode": "raw",
       "raw": "{}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('WEBHOOK_DELIVERY_NOT_REPLAYABLE', () =>",
         "    pm.expect(pm.response.json().code).to.eql('WEBHOOK_DELIVERY_NOT_REPLAYABLE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Rotate the secret: an integer bump, and a new secret (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}/rotate-secret",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}",
        "rotate-secret"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Nothing is re-encrypted because nothing was encrypted. secret_version goes to 2 and the new secret is derived from it.\n\nTHE OLD ONE KEEPS SIGNING FOR 24 HOURS, and previousSecretExpiresAt below is that window. A single signature cannot verify under two secrets, so a merchant who rotates before deploying their new verifier would otherwise see every delivery fail. Inside the window the header carries TWO v1= values, current first -- which makes it a superset of what ProviderCallbackSignatureFilter parses, and a merchant must check whether ANY matches (ADR-028 section 4).",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"fromVersion\": 1\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "",
         "pm.test('version 2', () => pm.expect(b.secretVersion).to.eql(2));",
         "pm.test('a different secret', () =>",
         "    pm.expect(b.secret).to.not.eql(pm.collectionVariables.get('whSecret')));",
         "pm.test('and a 24 hour window in which the old one still signs', () =>",
         "    pm.expect(b.previousSecretExpiresAt).to.not.be.null);",
         "",
         "pm.collectionVariables.set('whRotatedSecret', b.secret);"
        ]
       }
      }
     ]
    },
    {
     "name": "Rotating again from the same version returns the same secret: idempotent by construction (200)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}/rotate-secret",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}",
        "rotate-secret"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "THE REQUEST THAT JUSTIFIES KEEPING THIS ROUTE OFF THE IDEMPOTENCY FILTER.\n\nThe caller names the version it believes is current. Asked to rotate from a version that is already spent, the aggregate returns unchanged and the service re-derives the same secret -- so a client that lost the first response can safely ask again and receive the secret that is actually in force. No stored response body, no secret in idempotency_records, and no second bump that would invalidate a secret the merchant never received.\n\nWatch the version below: still 2, not 3.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"fromVersion\": 1\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "",
         "pm.test('still version 2 -- it did not bump again', () =>",
         "    pm.expect(b.secretVersion).to.eql(2));",
         "pm.test('and the same secret came back', () =>",
         "    pm.expect(b.secret).to.eql(pm.collectionVariables.get('whRotatedSecret')));"
        ]
       }
      }
     ]
    },
    {
     "name": "Rotating from a version that was never current is refused (409)",
     "request": {
      "method": "POST",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}/rotate-secret",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}",
        "rotate-secret"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The caller is working from a view of this endpoint that never existed, and a blind bump would invalidate a secret somebody is signing with. The two versions the endpoint will accept are the current one and the one it just replaced; everything else is a conflict.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"fromVersion\": 7\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('409 Conflict', () => pm.response.to.have.status(409));",
         "pm.test('WEBHOOK_SECRET_VERSION_MISMATCH', () =>",
         "    pm.expect(pm.response.json().code).to.eql('WEBHOOK_SECRET_VERSION_MISMATCH'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Resubscribing REPLACES the list rather than adding to it (200)",
     "request": {
      "method": "PATCH",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "A replacement, so a merchant unsubscribes by sending a shorter list rather than by needing a second verb. payment.succeeded is gone after this.\n\nAn EMPTY list is refused: an endpoint subscribed to nothing sits ACTIVE and silently receives nothing forever, which is a mistake that looks like a configuration.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"subscriptions\": [\n    \"order.paid\"\n  ]\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('exactly what was sent, nothing retained', () =>",
         "    pm.expect(pm.response.json().subscriptions).to.eql(['order.paid']));",
         "pm.test('the secret is NOT in this response', () =>",
         "    pm.expect(pm.response.json().secret).to.be.null);"
        ]
       }
      }
     ]
    },
    {
     "name": "Disable it: reversible, and the fan-out stops immediately (200)",
     "request": {
      "method": "PATCH",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "The same state PayMesh reaches on its own after 20 consecutive DEAD deliveries -- one dead delivery being one that spent its whole five-attempt budget, not one failed attempt. Confusing those two is a factor of five in when an endpoint disables, which is why the aggregate decides it rather than the caller.\n\nRequested as a value on one field because it is one field with two settings; the aggregate still moves through disable() rather than having a status written into it.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"status\": \"DISABLED\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('DISABLED', () => pm.expect(pm.response.json().status).to.eql('DISABLED'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Re-enable it, and the failure streak resets (200)",
     "request": {
      "method": "PATCH",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}"
       ]
      },
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       },
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "description": "Enabling clears consecutive_failures, which the aggregate decides. A merchant who fixed their endpoint and turned it back on should not be one dead delivery away from being disabled again.",
      "body": {
       "mode": "raw",
       "raw": "{\n  \"status\": \"ACTIVE\"\n}"
      }
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "pm.test('ACTIVE', () => pm.expect(pm.response.json().status).to.eql('ACTIVE'));"
        ]
       }
      }
     ]
    },
    {
     "name": "Another merchant's endpoint does not exist as far as you are concerned (404)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}/deliveries",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}",
        "deliveries"
       ]
      },
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{otherAccessToken}}"
       }
      ],
      "description": "A real endpoint id, a real token, a different tenant. 404 and not 403: a 403 would confirm the id exists and turn this route into an oracle for enumerating other merchants' endpoints (ADR-007).\n\nThe endpoint is resolved before the deliveries are read for the same reason -- an empty list would leak the same bit more quietly."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));",
         "pm.test('WEBHOOK_ENDPOINT_NOT_FOUND', () =>",
         "    pm.expect(pm.response.json().code).to.eql('WEBHOOK_ENDPOINT_NOT_FOUND'));"
        ]
       }
      }
     ]
    },
    {
     "name": "No token, no webhook endpoints (401)",
     "request": {
      "method": "GET",
      "url": {
       "raw": "{{baseUrl}}/api/v1/webhook-endpoints/{{whEndpointId}}/deliveries",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "webhook-endpoints",
        "{{whEndpointId}}",
        "deliveries"
       ]
      },
      "header": [],
      "description": "Nothing under /api/v1 is anonymous except merchant registration and auth."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));"
        ]
       }
      }
     ]
    }
   ]
  },
  {
   "name": "Settlement (money leaves the platform, ADR-032)",
   "description": "The HTTP surface of Settlement. The cut->submit->pay loop itself is scheduled and off under `dev`, and there is no route to cut a batch by hand, so this folder covers what a merchant can actually call: the payout config, the settlements read model, and the signature guard on the payout callback. The money movement is proven by SettlementLoopIntegrationTest, not here.",
   "item": [
    {
     "name": "Set the payout config: destination, minimum and no holding period (200)",
     "request": {
      "method": "PUT",
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\"holdingPeriodSeconds\": 0, \"payoutDestination\": \"acct_settlement_demo\", \"minimumPayoutMinor\": 1}"
      },
      "url": {
       "raw": "{{baseUrl}}/api/v1/settlement-config",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "settlement-config"
       ]
      },
      "description": "PUT replaces every setting, it does not merge. A destination makes the merchant payable; a zero holding period means 'release on the next run'; a minimum of one means 'any positive net qualifies'."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('destination round-trips', () => pm.expect(b.payoutDestination).to.eql('acct_settlement_demo'));",
         "pm.test('minimum round-trips', () => pm.expect(b.minimumPayoutMinor).to.eql(1));",
         "pm.test('holding period is zero', () => pm.expect(b.holdingPeriodSeconds).to.eql(0));",
         "pm.test('no longer the default', () => pm.expect(b.isDefault).to.eql(false));"
        ]
       }
      }
     ]
    },
    {
     "name": "Read the config back (200)",
     "request": {
      "method": "GET",
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "url": {
       "raw": "{{baseUrl}}/api/v1/settlement-config",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "settlement-config"
       ]
      },
      "description": "What was just set, read back. isDefault is false because a row now exists for this merchant."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('destination persisted', () => pm.expect(b.payoutDestination).to.eql('acct_settlement_demo'));",
         "pm.test('not the default', () => pm.expect(b.isDefault).to.eql(false));"
        ]
       }
      }
     ]
    },
    {
     "name": "Reject a payout destination longer than 80 chars (400)",
     "request": {
      "method": "PUT",
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\"holdingPeriodSeconds\": 0, \"payoutDestination\": \"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\", \"minimumPayoutMinor\": 1}"
      },
      "url": {
       "raw": "{{baseUrl}}/api/v1/settlement-config",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "settlement-config"
       ]
      },
      "description": "Boundary validation on the request record: the destination is @Size(max=80)."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));"
        ]
       }
      }
     ]
    },
    {
     "name": "Reject a minimum below one (400)",
     "request": {
      "method": "PUT",
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       },
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\"holdingPeriodSeconds\": 0, \"payoutDestination\": \"acct_settlement_demo\", \"minimumPayoutMinor\": 0}"
      },
      "url": {
       "raw": "{{baseUrl}}/api/v1/settlement-config",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "settlement-config"
       ]
      },
      "description": "A minimum of zero would be indistinguishable from no minimum. The request record is @Min(1); null (omitted) is what means 'the platform's smallest'."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('400 Bad Request', () => pm.response.to.have.status(400));"
        ]
       }
      }
     ]
    },
    {
     "name": "List this merchant's settlements (200, envelope)",
     "request": {
      "method": "GET",
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "url": {
       "raw": "{{baseUrl}}/api/v1/settlements",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "settlements"
       ]
      },
      "description": "The batches this merchant has been paid. No batch can be cut over HTTP under dev, so the list is usually empty -- what matters is the shape: a `settlements` array, never a 404 for a merchant who has none."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('200 OK', () => pm.response.to.have.status(200));",
         "const b = pm.response.json();",
         "pm.test('has a settlements array', () => pm.expect(b.settlements).to.be.an('array'));"
        ]
       }
      }
     ]
    },
    {
     "name": "A settlement that does not exist (404, and it does not say which)",
     "request": {
      "method": "GET",
      "header": [
       {
        "key": "Authorization",
        "value": "Bearer {{accessToken}}"
       }
      ],
      "url": {
       "raw": "{{baseUrl}}/api/v1/settlements/stl_00000000-0000-0000-0000-000000000000",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "settlements",
        "stl_00000000-0000-0000-0000-000000000000"
       ]
      },
      "description": "A well-formed id that names nothing is 404, and so is a malformed one -- telling a caller their id is invalid rather than absent leaks whether the format is right. Another merchant's batch answers the same 404, never 403."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('404 Not Found', () => pm.response.to.have.status(404));"
        ]
       }
      }
     ]
    },
    {
     "name": "No token, no settlements (401)",
     "request": {
      "method": "GET",
      "header": [],
      "url": {
       "raw": "{{baseUrl}}/api/v1/settlements",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "api",
        "v1",
        "settlements"
       ]
      },
      "description": "Everything under /api/v1 is authenticated by default; the settlements route needs no rule of its own."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));"
        ]
       }
      }
     ]
    },
    {
     "name": "Refuse an unsigned payout callback (401)",
     "request": {
      "method": "POST",
      "header": [
       {
        "key": "Content-Type",
        "value": "application/json"
       }
      ],
      "body": {
       "mode": "raw",
       "raw": "{\"payoutId\": \"po_00000000-0000-0000-0000-000000000000\", \"externalEventId\": \"evt_unsigned\", \"outcome\": \"SUCCEEDED\"}"
      },
      "url": {
       "raw": "{{baseUrl}}/internal/v1/payout-callbacks/SIMULATOR",
       "host": [
        "{{baseUrl}}"
       ],
       "path": [
        "internal",
        "v1",
        "payout-callbacks",
        "SIMULATOR"
       ]
      },
      "description": "BANK_CASH only moves on a signed callback. The route is permitAll at the security layer and guarded by ProviderCallbackSignatureFilter instead -- an HMAC over the raw body -- so an unsigned POST is rejected before any handler runs, exactly like the refund callback route."
     },
     "event": [
      {
       "listen": "test",
       "script": {
        "type": "text/javascript",
        "exec": [
         "pm.test('401 Unauthorized', () => pm.response.to.have.status(401));"
        ]
       }
      }
     ]
    }
   ]
  }
 ]
}
